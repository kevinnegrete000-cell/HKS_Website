console.info('[HKS] Catenary Relationships v3.2 loaded — falling weights build');
const archiveItems = [
  {
    "id": "obj-7593-3dp-01",
    "archiveCode": "OBJ-7593-3DP-01",
    "title": "3D Print Study 01",
    "location": "Digital Object Archive",
    "locationCode": "7593",
    "typology": "Additive Manufacturing Research",
    "prototypeType": "Textured 3D Print Scan",
    "material": "Printed Composite / Scanned Surface",
    "fabricationMethod": "Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the 3D Print collection.",
    "introduction": "3D Print Study 01 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.7593.01",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 4,
    "orbitAngleDeg": -42,
    "model": {
      "obj": "assets/models/3d-print-01/model.obj",
      "mtl": "assets/models/3d-print-01/model.mtl",
      "diffuse": "assets/models/3d-print-01/textures/diffuse.jpg",
      "normal": "assets/models/3d-print-01/textures/normal.jpg",
      "ao": "assets/models/3d-print-01/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-2710-3dp-02",
    "archiveCode": "OBJ-2710-3DP-02",
    "title": "3D Print Study 02",
    "location": "Digital Object Archive",
    "locationCode": "2710",
    "typology": "Additive Manufacturing Research",
    "prototypeType": "Textured 3D Print Scan",
    "material": "Printed Composite / Scanned Surface",
    "fabricationMethod": "Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the 3D Print collection.",
    "introduction": "3D Print Study 02 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.2710.02",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 5,
    "orbitAngleDeg": -7,
    "model": {
      "obj": "assets/models/3d-print-02/model.obj",
      "mtl": "assets/models/3d-print-02/model.mtl",
      "diffuse": "assets/models/3d-print-02/textures/diffuse.jpg",
      "normal": "assets/models/3d-print-02/textures/normal.jpg",
      "ao": "assets/models/3d-print-02/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-7460-3dp-03",
    "archiveCode": "OBJ-7460-3DP-03",
    "title": "3D Print Study 03",
    "location": "Digital Object Archive",
    "locationCode": "7460",
    "typology": "Additive Manufacturing Research",
    "prototypeType": "Textured 3D Print Scan",
    "material": "Printed Composite / Scanned Surface",
    "fabricationMethod": "Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the 3D Print collection.",
    "introduction": "3D Print Study 03 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.7460.03",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 6,
    "orbitAngleDeg": 24,
    "model": {
      "obj": "assets/models/3d-print-03/model.obj",
      "mtl": "assets/models/3d-print-03/model.mtl",
      "diffuse": "assets/models/3d-print-03/textures/diffuse.jpg",
      "normal": "assets/models/3d-print-03/textures/normal.jpg",
      "ao": "assets/models/3d-print-03/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-0737-3dp-04",
    "archiveCode": "OBJ-0737-3DP-04",
    "title": "3D Print Study 04",
    "location": "Digital Object Archive",
    "locationCode": "0737",
    "typology": "Additive Manufacturing Research",
    "prototypeType": "Textured 3D Print Scan",
    "material": "Printed Composite / Scanned Surface",
    "fabricationMethod": "Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the 3D Print collection.",
    "introduction": "3D Print Study 04 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.0737.04",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 7,
    "orbitAngleDeg": 50,
    "model": {
      "obj": "assets/models/3d-print-04/model.obj",
      "mtl": "assets/models/3d-print-04/model.mtl",
      "diffuse": "assets/models/3d-print-04/textures/diffuse.jpg",
      "normal": "assets/models/3d-print-04/textures/normal.jpg",
      "ao": "assets/models/3d-print-04/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-0368-big-01",
    "archiveCode": "OBJ-0368-BIG-01",
    "title": "Large-Scale Prototype Study 01",
    "location": "Digital Object Archive",
    "locationCode": "0368",
    "typology": "Large Project / Spatial Prototype",
    "prototypeType": "Textured Large-Scale Scan",
    "material": "Mapped Architectural Assembly",
    "fabricationMethod": "Digital Mesh / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Large-Scale Prototype collection.",
    "introduction": "Large-Scale Prototype Study 01 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.0368.05",
    "year": 2026,
    "importance": 5,
    "orbitIndex": 8,
    "orbitAngleDeg": 77,
    "model": {
      "obj": "assets/models/big-projects-01/model.obj",
      "mtl": "assets/models/big-projects-01/model.mtl",
      "diffuse": "assets/models/big-projects-01/textures/diffuse.jpg",
      "normal": "assets/models/big-projects-01/textures/normal.jpg",
      "ao": "assets/models/big-projects-01/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-4071-col-01",
    "archiveCode": "OBJ-4071-COL-01",
    "title": "Column Print Study 01",
    "location": "Digital Object Archive",
    "locationCode": "4071",
    "typology": "Structural Column Research",
    "prototypeType": "Textured Column Scan",
    "material": "Printed Concrete / Composite",
    "fabricationMethod": "Robotic Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Column Print collection.",
    "introduction": "Column Print Study 01 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.4071.06",
    "year": 2026,
    "importance": 5,
    "orbitIndex": 3,
    "orbitAngleDeg": 112,
    "model": {
      "obj": "assets/models/column-print-01/model.obj",
      "mtl": "assets/models/column-print-01/model.mtl",
      "diffuse": "assets/models/column-print-01/textures/diffuse.jpg",
      "normal": "assets/models/column-print-01/textures/normal.jpg",
      "ao": "assets/models/column-print-01/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-6628-col-02",
    "archiveCode": "OBJ-6628-COL-02",
    "title": "Column Print Study 02",
    "location": "Digital Object Archive",
    "locationCode": "6628",
    "typology": "Structural Column Research",
    "prototypeType": "Textured Column Scan",
    "material": "Printed Concrete / Composite",
    "fabricationMethod": "Robotic Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Column Print collection.",
    "introduction": "Column Print Study 02 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.6628.07",
    "year": 2026,
    "importance": 5,
    "orbitIndex": 4,
    "orbitAngleDeg": 145,
    "model": {
      "obj": "assets/models/column-print-02/model.obj",
      "mtl": "assets/models/column-print-02/model.mtl",
      "diffuse": "assets/models/column-print-02/textures/diffuse.jpg",
      "normal": "assets/models/column-print-02/textures/normal.jpg",
      "ao": "assets/models/column-print-02/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-8171-col-03",
    "archiveCode": "OBJ-8171-COL-03",
    "title": "Column Print Study 03",
    "location": "Digital Object Archive",
    "locationCode": "8171",
    "typology": "Structural Column Research",
    "prototypeType": "Textured Column Scan",
    "material": "Printed Concrete / Composite",
    "fabricationMethod": "Robotic Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Column Print collection.",
    "introduction": "Column Print Study 03 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.8171.08",
    "year": 2026,
    "importance": 5,
    "orbitIndex": 5,
    "orbitAngleDeg": 177,
    "model": {
      "obj": "assets/models/column-print-03/model.obj",
      "mtl": "assets/models/column-print-03/model.mtl",
      "diffuse": "assets/models/column-print-03/textures/diffuse.jpg",
      "normal": "assets/models/column-print-03/textures/normal.jpg",
      "ao": "assets/models/column-print-03/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-0858-col-04",
    "archiveCode": "OBJ-0858-COL-04",
    "title": "Column Print Study 04",
    "location": "Digital Object Archive",
    "locationCode": "0858",
    "typology": "Structural Column Research",
    "prototypeType": "Textured Column Scan",
    "material": "Printed Concrete / Composite",
    "fabricationMethod": "Robotic Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Column Print collection.",
    "introduction": "Column Print Study 04 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.0858.09",
    "year": 2026,
    "importance": 5,
    "orbitIndex": 6,
    "orbitAngleDeg": 207,
    "model": {
      "obj": "assets/models/column-print-04/model.obj",
      "mtl": "assets/models/column-print-04/model.mtl",
      "diffuse": "assets/models/column-print-04/textures/diffuse.jpg",
      "normal": "assets/models/column-print-04/textures/normal.jpg",
      "ao": "assets/models/column-print-04/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-9162-col-05",
    "archiveCode": "OBJ-9162-COL-05",
    "title": "Column Print Study 05",
    "location": "Digital Object Archive",
    "locationCode": "9162",
    "typology": "Structural Column Research",
    "prototypeType": "Textured Column Scan",
    "material": "Printed Concrete / Composite",
    "fabricationMethod": "Robotic Additive Fabrication / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Column Print collection.",
    "introduction": "Column Print Study 05 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.9162.10",
    "year": 2026,
    "importance": 5,
    "orbitIndex": 7,
    "orbitAngleDeg": 235,
    "model": {
      "obj": "assets/models/column-print-05/model.obj",
      "mtl": "assets/models/column-print-05/model.mtl",
      "diffuse": "assets/models/column-print-05/textures/diffuse.jpg",
      "normal": "assets/models/column-print-05/textures/normal.jpg",
      "ao": "assets/models/column-print-05/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-4815-con-01",
    "archiveCode": "OBJ-4815-CON-01",
    "title": "Concrete Print Study 01",
    "location": "Digital Object Archive",
    "locationCode": "4815",
    "typology": "Concrete Fabrication Research",
    "prototypeType": "Textured Concrete Scan",
    "material": "3D-Printed Concrete",
    "fabricationMethod": "Concrete Extrusion / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Concrete Print collection.",
    "introduction": "Concrete Print Study 01 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.4815.11",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 3,
    "orbitAngleDeg": 270,
    "model": {
      "obj": "assets/models/concrete-print-01/model.obj",
      "mtl": "assets/models/concrete-print-01/model.mtl",
      "diffuse": "assets/models/concrete-print-01/textures/diffuse.jpg",
      "normal": "assets/models/concrete-print-01/textures/normal.jpg",
      "ao": "assets/models/concrete-print-01/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-8682-con-02",
    "archiveCode": "OBJ-8682-CON-02",
    "title": "Concrete Print Study 02",
    "location": "Digital Object Archive",
    "locationCode": "8682",
    "typology": "Concrete Fabrication Research",
    "prototypeType": "Textured Concrete Scan",
    "material": "3D-Printed Concrete",
    "fabricationMethod": "Concrete Extrusion / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Concrete Print collection.",
    "introduction": "Concrete Print Study 02 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.8682.12",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 4,
    "orbitAngleDeg": 301,
    "model": {
      "obj": "assets/models/concrete-print-02/model.obj",
      "mtl": "assets/models/concrete-print-02/model.mtl",
      "diffuse": "assets/models/concrete-print-02/textures/diffuse.jpg",
      "normal": "assets/models/concrete-print-02/textures/normal.jpg",
      "ao": "assets/models/concrete-print-02/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-3899-con-03",
    "archiveCode": "OBJ-3899-CON-03",
    "title": "Concrete Print Study 03",
    "location": "Digital Object Archive",
    "locationCode": "3899",
    "typology": "Concrete Fabrication Research",
    "prototypeType": "Textured Concrete Scan",
    "material": "3D-Printed Concrete",
    "fabricationMethod": "Concrete Extrusion / Photogrammetry",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Concrete Print collection.",
    "introduction": "Concrete Print Study 03 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.3899.13",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 5,
    "orbitAngleDeg": 331,
    "model": {
      "obj": "assets/models/concrete-print-03/model.obj",
      "mtl": "assets/models/concrete-print-03/model.mtl",
      "diffuse": "assets/models/concrete-print-03/textures/diffuse.jpg",
      "normal": "assets/models/concrete-print-03/textures/normal.jpg",
      "ao": "assets/models/concrete-print-03/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-4010-win-01",
    "archiveCode": "OBJ-4010-WIN-01",
    "title": "Window Profile Study 01",
    "location": "Digital Object Archive",
    "locationCode": "4010",
    "typology": "Envelope System / Window Assembly",
    "prototypeType": "Textured Window Profile Scan",
    "material": "Mapped Architectural Component",
    "fabricationMethod": "Digital Mesh / Material Mapping",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Window Profile collection.",
    "introduction": "Window Profile Study 01 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.4010.14",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 2,
    "orbitAngleDeg": -78,
    "model": {
      "obj": "assets/models/window-profile-01/model.obj",
      "mtl": "assets/models/window-profile-01/model.mtl",
      "diffuse": "assets/models/window-profile-01/textures/diffuse.jpg",
      "normal": "assets/models/window-profile-01/textures/normal.jpg",
      "ao": "assets/models/window-profile-01/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  },
  {
    "id": "obj-6750-win-02",
    "archiveCode": "OBJ-6750-WIN-02",
    "title": "Window Profile Study 02",
    "location": "Digital Object Archive",
    "locationCode": "6750",
    "typology": "Envelope System / Window Assembly",
    "prototypeType": "Textured Window Profile Scan",
    "material": "Mapped Architectural Component",
    "fabricationMethod": "Digital Mesh / Material Mapping",
    "projectPhase": "Interactive Archive Model",
    "description": "Interactive textured model from the Window Profile collection.",
    "introduction": "Window Profile Study 02 is presented as an interactive archive object with its supplied diffuse, normal, and ambient-occlusion maps. Drag to orbit, scroll to zoom, or let the model rotate slowly while reviewing its archive information.",
    "accessionNumber": "OBJ.2026.6750.15",
    "year": 2026,
    "importance": 4,
    "orbitIndex": 3,
    "orbitAngleDeg": -20,
    "model": {
      "obj": "assets/models/window-profile-02/model.obj",
      "mtl": "assets/models/window-profile-02/model.mtl",
      "diffuse": "assets/models/window-profile-02/textures/diffuse.jpg",
      "normal": "assets/models/window-profile-02/textures/normal.jpg",
      "ao": "assets/models/window-profile-02/textures/ao.jpg",
      "rotation": [
        -1.5707963268,
        0,
        0
      ]
    }
  }
];

const ATLAS_LOCATIONS={
  'obj-7593-3dp-01':{city:'Los Angeles',country:'USA',lat:34.0522,lon:-118.2437},
  'obj-2710-3dp-02':{city:'Dallas',country:'USA',lat:32.7767,lon:-96.7970},
  'obj-7460-3dp-03':{city:'Seattle',country:'USA',lat:47.6062,lon:-122.3321},
  'obj-0737-3dp-04':{city:'Mexico City',country:'Mexico',lat:19.4326,lon:-99.1332},
  'obj-0368-big-01':{city:'London',country:'United Kingdom',lat:51.5072,lon:-0.1276},
  'obj-4071-col-01':{city:'Chicago',country:'USA',lat:41.8781,lon:-87.6298},
  'obj-6628-col-02':{city:'Toronto',country:'Canada',lat:43.6532,lon:-79.3832},
  'obj-8171-col-03':{city:'Copenhagen',country:'Denmark',lat:55.6761,lon:12.5683},
  'obj-0858-col-04':{city:'Barcelona',country:'Spain',lat:41.3874,lon:2.1686},
  'obj-9162-col-05':{city:'Dubai',country:'UAE',lat:25.2048,lon:55.2708},
  'obj-4815-con-01':{city:'São Paulo',country:'Brazil',lat:-23.5505,lon:-46.6333},
  'obj-8682-con-02':{city:'Cape Town',country:'South Africa',lat:-33.9249,lon:18.4241},
  'obj-3899-con-03':{city:'Singapore',country:'Singapore',lat:1.3521,lon:103.8198},
  'obj-4010-win-01':{city:'Tokyo',country:'Japan',lat:35.6762,lon:139.6503},
  'obj-6750-win-02':{city:'Sydney',country:'Australia',lat:-33.8688,lon:151.2093}
};
archiveItems.forEach(item=>{item.atlas=ATLAS_LOCATIONS[item.id]||{city:'Unassigned',country:'',lat:0,lon:0};});

const relationships = [
  {
    "source": "obj-7593-3dp-01",
    "target": "obj-2710-3dp-02",
    "category": "Fabrication",
    "description": "Sequential objects within the 3D Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-2710-3dp-02",
    "target": "obj-7460-3dp-03",
    "category": "Fabrication",
    "description": "Sequential objects within the 3D Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-7460-3dp-03",
    "target": "obj-0737-3dp-04",
    "category": "Fabrication",
    "description": "Sequential objects within the 3D Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-4071-col-01",
    "target": "obj-6628-col-02",
    "category": "Fabrication",
    "description": "Sequential objects within the Column Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-6628-col-02",
    "target": "obj-8171-col-03",
    "category": "Fabrication",
    "description": "Sequential objects within the Column Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-8171-col-03",
    "target": "obj-0858-col-04",
    "category": "Fabrication",
    "description": "Sequential objects within the Column Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-0858-col-04",
    "target": "obj-9162-col-05",
    "category": "Fabrication",
    "description": "Sequential objects within the Column Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-4815-con-01",
    "target": "obj-8682-con-02",
    "category": "Fabrication",
    "description": "Sequential objects within the Concrete Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-8682-con-02",
    "target": "obj-3899-con-03",
    "category": "Fabrication",
    "description": "Sequential objects within the Concrete Print study series.",
    "strength": 0.82
  },
  {
    "source": "obj-4010-win-01",
    "target": "obj-6750-win-02",
    "category": "Geometry",
    "description": "Sequential objects within the Window Profile study series.",
    "strength": 0.82
  },
  {
    "source": "obj-7593-3dp-01",
    "target": "obj-4815-con-01",
    "category": "Material",
    "description": "Related additive-manufacturing surface and material studies.",
    "strength": 0.58
  },
  {
    "source": "obj-0368-big-01",
    "target": "obj-0858-col-04",
    "category": "Project Phase",
    "description": "Large-scale prototype research connected to the column study collection.",
    "strength": 0.63
  },
  {
    "source": "obj-3899-con-03",
    "target": "obj-4010-win-01",
    "category": "Typology",
    "description": "Transition from material-scale fabrication to envelope-component research.",
    "strength": 0.41
  }
];
const orbitCenters = [
  {
    "id": "center-fabrication",
    "label": "Fabrication Systems",
    "x": 260,
    "y": 610,
    "maxRadius": 420,
    "ringCount": 5
  },
  {
    "id": "center-material",
    "label": "Material Research",
    "x": 1030,
    "y": 230,
    "maxRadius": 340,
    "ringCount": 4
  },
  {
    "id": "center-typology",
    "label": "Typological Studies",
    "x": 820,
    "y": 800,
    "maxRadius": 290,
    "ringCount": 4
  },
  {
    "id": "center-geometry",
    "label": "Geometry Experiments",
    "x": 1440,
    "y": 570,
    "maxRadius": 330,
    "ringCount": 4
  }
];


let THREE=null;
let OBJLoader=null;
let OrbitControls=null;

const $=selector=>document.querySelector(selector);
const $$=selector=>[...document.querySelectorAll(selector)];
const prefersReducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;

const state={
  view:'observatory',
  selected:null,
  scale:window.innerWidth<760?.68:.86,
  x:0,
  y:0,
  dragging:false,
  lastX:0,
  lastY:0,
  filters:new Set(['Material','Fabrication','Typology','Geometry','Project Phase']),
  returnTransform:null,
  orbitPaused:false,
  followSelected:false,
  focusTargetX:0,
  focusTargetY:0
};

const ORBIT_CENTER={x:-455,y:205};
const ORBIT_TRACKS=[
  {rx:230,ry:142,tilt:-12,spin:.018},
  {rx:365,ry:216,tilt:-10,spin:-.014},
  {rx:500,ry:292,tilt:-8,spin:.011},
  {rx:635,ry:368,tilt:-6,spin:-.009},
  {rx:770,ry:444,tilt:-4,spin:.0075},
  {rx:905,ry:520,tilt:-2,spin:-.0063},
  {rx:1040,ry:596,tilt:0,spin:.0052},
  {rx:1175,ry:672,tilt:2,spin:-.0044},
  {rx:1310,ry:748,tilt:4,spin:.0038},
  {rx:1445,ry:824,tilt:6,spin:-.0033},
  {rx:1580,ry:900,tilt:8,spin:.0029},
  {rx:1715,ry:976,tilt:10,spin:-.0026}
];
const START_ANGLES=[-72,-54,-39,-25,-11,4,20,38,57,78,102,128,156,185,214,244,274,304,330,348,28];
const RING_PATTERN=[4,5,6,3,2,5,4,6,3,7,5,4,7,3,6,2,5,7,4,6,3];
const nodeRefs=new Map();
const relationshipRefs=[];
const ringRefs=[];
let selectionTether=null;
let orbitAnimationFrame=null;
let orbitClock=0;
let lastOrbitTime=performance.now();
let canvasAnimationFrame=null;
let modelViewer=null;
let relationshipViewer=null;
let atlasViewer=null;
let atlasPendingFocus=null;
let atlasTransitioning=false;
const relationshipState={
  selectedId:archiveItems[0]?.id||null,
  hoveredId:null,
  mode:'all'
};

archiveItems.forEach((item,index)=>{
  item.orbitIndex=Number.isInteger(item.orbitIndex)?item.orbitIndex:RING_PATTERN[index%RING_PATTERN.length];
  const startAngle=Number.isFinite(item.orbitAngleDeg)?item.orbitAngleDeg:START_ANGLES[index%START_ANGLES.length];
  item.orbitTheta=startAngle*Math.PI/180;
  const direction=index%2===0?1:-1;
  item.orbitSpeed=direction*(.0105+(index%5)*.0018);
  item.x=0;
  item.y=0;
});

$('#itemCount').textContent=String(archiveItems.length).padStart(3,'0');

function commitView(name){
  if(state.view==='relationships'&&name!=='relationships')destroyRelationshipNetwork();
  if(state.view==='atlas'&&name!=='atlas')destroyAtlasView();
  state.view=name;
  $$('.view').forEach(view=>view.classList.toggle('active-view',view.id===name));
  $$('.nav-btn').forEach(button=>button.classList.toggle('active',button.dataset.view===name));
  closeDetail(false);
  if(name==='relationships')requestAnimationFrame(()=>initRelationshipNetwork());
  if(name==='atlas')requestAnimationFrame(()=>initAtlasView());
}

function beginAtlasTransition(){
  if(atlasTransitioning)return;
  atlasTransitioning=true;
  const overlay=$('#atlasTransition');
  overlay?.classList.remove('leaving');
  overlay?.classList.add('active');
  overlay?.setAttribute('aria-hidden','false');
  document.body.classList.add('atlas-morphing');
  window.setTimeout(()=>commitView('atlas'),prefersReducedMotion?0:610);
  window.setTimeout(()=>overlay?.classList.add('leaving'),prefersReducedMotion?30:1270);
  window.setTimeout(()=>{
    overlay?.classList.remove('active','leaving');
    overlay?.setAttribute('aria-hidden','true');
    document.body.classList.remove('atlas-morphing');
    atlasTransitioning=false;
  },prefersReducedMotion?80:1740);
}

function setView(name,options={}){
  if(name==='atlas'&&state.view==='observatory'&&!options.skipAtlasTransition){
    beginAtlasTransition();
    return;
  }
  commitView(name);
}

$$('[data-view]').forEach(element=>element.addEventListener('click',event=>{
  event.preventDefault();
  setView(element.dataset.view);
}));

function applyTransform(){
  const transform=`translate(${state.x}px,${state.y}px) scale(${state.scale})`;
  $('#orbitSvg').style.transform=transform;
  $('#nodesLayer').style.transform=transform;
}

function svgEl(tag,attrs={}){
  const element=document.createElementNS('http://www.w3.org/2000/svg',tag);
  Object.entries(attrs).forEach(([key,value])=>element.setAttribute(key,value));
  return element;
}

function rotatePoint(x,y,angle){
  const cos=Math.cos(angle),sin=Math.sin(angle);
  return {x:x*cos-y*sin,y:x*sin+y*cos};
}

function archiveNumber(item,index){
  const digits=(item.accessionNumber.match(/\d+/g)||[]).join('');
  return digits.slice(-3)||String(100+index);
}

function nodeLabelHTML(item){
  return `<span class="node-code"><strong>${item.archiveCode}</strong> <em>| ${item.year}</em></span>
    <span>${item.prototypeType}</span>
    <span>Accession No: ${item.accessionNumber}</span>
    <span>${item.title}</span>`;
}

function createSvgDefs(svg){
  const defs=svgEl('defs');
  const lineGlow=svgEl('filter',{id:'lineGlow',x:'-40%',y:'-40%',width:'180%',height:'180%'});
  lineGlow.appendChild(svgEl('feGaussianBlur',{stdDeviation:'1.4',result:'blur'}));
  const lineMerge=svgEl('feMerge');
  lineMerge.appendChild(svgEl('feMergeNode',{in:'blur'}));
  lineMerge.appendChild(svgEl('feMergeNode',{in:'SourceGraphic'}));
  lineGlow.appendChild(lineMerge);
  const pointGlow=svgEl('filter',{id:'pointGlow',x:'-100%',y:'-100%',width:'300%',height:'300%'});
  pointGlow.appendChild(svgEl('feGaussianBlur',{stdDeviation:'2.4',result:'blur'}));
  const pointMerge=svgEl('feMerge');
  pointMerge.appendChild(svgEl('feMergeNode',{in:'blur'}));
  pointMerge.appendChild(svgEl('feMergeNode',{in:'SourceGraphic'}));
  pointGlow.appendChild(pointMerge);
  defs.append(lineGlow,pointGlow);
  svg.appendChild(defs);
}

function renderObservatory(){
  const svg=$('#orbitSvg');
  const layer=$('#nodesLayer');
  svg.innerHTML='';
  layer.innerHTML='';
  ringRefs.length=0;
  relationshipRefs.length=0;
  nodeRefs.clear();
  createSvgDefs(svg);

  ORBIT_TRACKS.forEach((track,index)=>{
    const base=svgEl('ellipse',{
      cx:ORBIT_CENTER.x,cy:ORBIT_CENTER.y,rx:track.rx,ry:track.ry,class:'orbit-track'
    });
    const sweep=svgEl('ellipse',{
      cx:ORBIT_CENTER.x,cy:ORBIT_CENTER.y,rx:track.rx,ry:track.ry,class:'orbit-sweep'
    });
    sweep.style.strokeDasharray=`${105+index*13} ${1160+index*175}`;
    svg.append(base,sweep);
    ringRefs.push({base,sweep,track});

    if(index>0){
      const tick=svgEl('circle',{r:index%3===0?2.6:1.7,class:'orbit-tick'});
      svg.appendChild(tick);
      ringRefs[index].tick=tick;
    }
  });

  relationships.slice(0,8).forEach(relationship=>{
    const path=svgEl('path',{class:'relationship-line'});
    svg.appendChild(path);
    relationshipRefs.push({relationship,path});
  });

  selectionTether=svgEl('path',{class:'selection-tether'});
  svg.appendChild(selectionTether);

  archiveItems.forEach((item,index)=>{
    const node=document.createElement('button');
    node.className='node';
    if(index%4===0||item.model)node.classList.add('has-field');
    if(item.model)node.classList.add('has-model');
    node.dataset.id=item.id;
    node.style.setProperty('--node-size',`${18+item.importance*4.5}px`);
    node.setAttribute('aria-label',item.title);
    node.innerHTML=`<span class="node-field"></span><span class="node-core"></span><span class="node-number">${archiveNumber(item,index)}</span><span class="node-label">${nodeLabelHTML(item)}</span>`;
    node.addEventListener('click',event=>{
      event.stopPropagation();
      focusItem(item.id);
    });
    layer.appendChild(node);
    nodeRefs.set(item.id,node);
  });

  applyTransform();
  if(!orbitAnimationFrame){
    lastOrbitTime=performance.now();
    orbitAnimationFrame=requestAnimationFrame(animateOrbitSystem);
  }
}

function updateOrbitPositions(){
  ringRefs.forEach((ref,index)=>{
    const rotation=(ref.track.tilt*Math.PI/180)+(orbitClock*ref.track.spin);
    const rotationDeg=rotation*180/Math.PI;
    const transform=`rotate(${rotationDeg} ${ORBIT_CENTER.x} ${ORBIT_CENTER.y})`;
    ref.base.setAttribute('transform',transform);
    ref.sweep.setAttribute('transform',transform);
    ref.sweep.style.strokeDashoffset=String(-orbitClock*(10+index*1.6));
    if(ref.tick){
      const theta=-1.18+index*.57;
      const raw={x:ref.track.rx*Math.cos(theta),y:ref.track.ry*Math.sin(theta)};
      const rotated=rotatePoint(raw.x,raw.y,rotation);
      ref.tick.setAttribute('cx',ORBIT_CENTER.x+rotated.x);
      ref.tick.setAttribute('cy',ORBIT_CENTER.y+rotated.y);
    }
  });

  archiveItems.forEach((item,index)=>{
    const track=ORBIT_TRACKS[item.orbitIndex];
    const ringRotation=(track.tilt*Math.PI/180)+(orbitClock*track.spin);
    const theta=item.orbitTheta+(orbitClock*item.orbitSpeed);
    const raw={x:track.rx*Math.cos(theta),y:track.ry*Math.sin(theta)};
    const rotated=rotatePoint(raw.x,raw.y,ringRotation);
    item.x=ORBIT_CENTER.x+rotated.x;
    item.y=ORBIT_CENTER.y+rotated.y;
    const node=nodeRefs.get(item.id);
    if(node){
      node.style.left=`${item.x}px`;
      node.style.top=`${item.y}px`;
      node.classList.toggle('label-left',item.x>310);
    }
  });

  // Once a project panel is open, keep its selected node visually anchored
  // while every orbit, sweep, relationship, and background node continues moving.
  if(state.followSelected&&state.selected&&!canvasAnimationFrame){
    const selectedItem=archiveItems.find(entry=>entry.id===state.selected);
    const canvas=$('#canvas');
    if(selectedItem&&canvas){
      const rect=canvas.getBoundingClientRect();
      const targetX=state.focusTargetX||Math.min(rect.width*.28,430);
      const targetY=state.focusTargetY||rect.height*.53;
      state.x=targetX-rect.width/2-selectedItem.x*state.scale;
      state.y=targetY-rect.height/2-selectedItem.y*state.scale;
      applyTransform();
    }
  }

  relationshipRefs.forEach(({relationship,path})=>{
    const source=archiveItems.find(item=>item.id===relationship.source);
    const target=archiveItems.find(item=>item.id===relationship.target);
    if(!source||!target)return;
    const midX=(source.x+target.x)/2;
    const midY=(source.y+target.y)/2-32;
    path.setAttribute('d',`M ${source.x} ${source.y} Q ${midX} ${midY} ${target.x} ${target.y}`);
  });

  if(state.selected&&selectionTether){
    const item=archiveItems.find(entry=>entry.id===state.selected);
    if(item){
      selectionTether.classList.add('visible');
      selectionTether.setAttribute('d',`M ${item.x} ${item.y} L ${item.x+72} ${item.y-42} L ${item.x+490} ${item.y-42}`);
    }
  }else if(selectionTether){
    selectionTether.classList.remove('visible');
  }
}

function animateOrbitSystem(now){
  const delta=Math.min((now-lastOrbitTime)/1000,.05);
  lastOrbitTime=now;
  if(!state.orbitPaused&&!prefersReducedMotion)orbitClock+=delta;
  updateOrbitPositions();
  orbitAnimationFrame=requestAnimationFrame(animateOrbitSystem);
}

function animateCanvasTo(target,duration=620,onComplete){
  if(canvasAnimationFrame)cancelAnimationFrame(canvasAnimationFrame);
  const start={scale:state.scale,x:state.x,y:state.y};
  const started=performance.now();
  const ease=t=>1-Math.pow(1-t,3);
  const tick=now=>{
    const raw=Math.min(1,(now-started)/duration);
    const t=ease(raw);
    state.scale=start.scale+(target.scale-start.scale)*t;
    state.x=start.x+(target.x-start.x)*t;
    state.y=start.y+(target.y-start.y)*t;
    applyTransform();
    if(raw<1)canvasAnimationFrame=requestAnimationFrame(tick);
    else{
      canvasAnimationFrame=null;
      onComplete?.();
    }
  };
  canvasAnimationFrame=requestAnimationFrame(tick);
}

function focusItem(id){
  const item=archiveItems.find(entry=>entry.id===id);
  if(!item)return;
  if(state.view!=='observatory')setView('observatory');
  state.returnTransform={scale:state.scale,x:state.x,y:state.y};
  // Briefly hold the orbit only during the camera move. It resumes as soon
  // as the information panel opens, so the archive remains alive behind it.
  state.orbitPaused=true;
  state.followSelected=false;
  state.selected=id;
  $$('.node').forEach(node=>node.classList.toggle('selected',node.dataset.id===id));
  updateOrbitPositions();

  const canvas=$('#canvas');
  const rect=canvas.getBoundingClientRect();
  const focusScale=item.model?1.02:.95;
  const targetX=Math.min(rect.width*.28,430);
  const targetY=rect.height*.53;
  state.focusTargetX=targetX;
  state.focusTargetY=targetY;
  const x=targetX-rect.width/2-item.x*focusScale;
  const y=targetY-rect.height/2-item.y*focusScale;
  animateCanvasTo({scale:focusScale,x,y},700,()=>{
    openDetail(id);
    state.followSelected=true;
    state.orbitPaused=false;
  });
}

function metaRows(item){
  return [
    ['Year',item.year],
    ['Geographic Location Code',item.locationCode],
    ['Atlas Location',item.atlas?`${item.atlas.city}, ${item.atlas.country}`:'Unassigned'],
    ['Project Typology',item.typology],
    ['Prototype Type',item.prototypeType],
    ['Material',item.material],
    ['Fabrication Method',item.fabricationMethod],
    ['Project Phase',item.projectPhase],
    ['Archive Accession Number',item.accessionNumber]
  ].map(([label,value])=>`<div class="meta-row"><span>${label}</span><span>${value}</span></div>`).join('');
}

function actionButtons(){
  return `<div class="detail-actions">
    <button data-action="relationships">Explore Relationships</button>
    <button class="primary" data-action="timeline">View Timeline</button>
    <button data-action="atlas">View on Atlas</button>
  </div>`;
}

function openDetail(id){
  destroyModelViewer();
  state.selected=id;
  $$('.node').forEach(node=>node.classList.toggle('selected',node.dataset.id===id));
  const item=archiveItems.find(entry=>entry.id===id);
  if(!item)return;
  const panel=$('#detailPanel');
  panel.classList.toggle('model-mode',Boolean(item.model));

  const media=item.model?`
    <div class="model-stage-shell">
      <div id="modelViewport" class="model-viewport" aria-label="Interactive 3D model of ${item.title}"></div>
      <div id="modelStatus" class="model-status">Loading textured object…</div>
      <div class="model-controls-hint">Drag to orbit · Scroll to zoom · Auto-rotate enabled</div>
    </div>
    <div class="model-thumbnails">
      <img src="${item.model.diffuse}" alt="Diffuse material map">
      <img src="${item.model.normal}" alt="Normal material map">
      <img src="${item.model.ao}" alt="Ambient occlusion material map">
      <div class="texture-placeholder" aria-hidden="true"></div>
    </div>`:`
    <div class="archive-image"></div>
    <div class="model-thumbnails">
      <div class="texture-placeholder"></div><div class="texture-placeholder"></div>
      <div class="texture-placeholder"></div><div class="texture-placeholder"></div>
    </div>`;

  $('#detailContent').innerHTML=`<div class="detail-inner">
    <p class="detail-eyebrow">Confidential Project Code</p>
    <div class="detail-code-large">${item.archiveCode}</div>
    <div class="detail-subline">${item.prototypeType} / ${item.fabricationMethod} / ${item.material}</div>
    <div class="detail-content-grid">
      <div class="detail-media">${media}</div>
      <div class="detail-copy">
        <p>${item.introduction}</p>
        <h3>Prototype description</h3>
        <div class="meta">${metaRows(item)}</div>
      </div>
    </div>
    ${actionButtons()}
  </div>`;

  panel.classList.add('open');
  panel.setAttribute('aria-hidden','false');
  panel.querySelector('[data-action="relationships"]')?.addEventListener('click',()=>setView('relationships'));
  panel.querySelector('[data-action="timeline"]')?.addEventListener('click',()=>setView('timeline'));
  panel.querySelector('[data-action="atlas"]')?.addEventListener('click',()=>showItemOnAtlas(id));
  if(item.model)requestAnimationFrame(()=>initModelViewer(item));
}

function closeDetail(restore=true){
  destroyModelViewer();
  const panel=$('#detailPanel');
  panel.classList.remove('open','model-mode');
  panel.setAttribute('aria-hidden','true');
  state.followSelected=false;
  state.orbitPaused=false;
  state.selected=null;
  $$('.node').forEach(node=>node.classList.remove('selected'));
  updateOrbitPositions();

  if(restore&&state.returnTransform){
    const previous=state.returnTransform;
    state.returnTransform=null;
    animateCanvasTo(previous,560);
  }else{
    state.returnTransform=null;
  }
}

async function ensureThree(){
  if(THREE&&OBJLoader&&OrbitControls)return;
  const [threeModule,objModule,controlsModule]=await Promise.all([
    import('three'),
    import('three/addons/loaders/OBJLoader.js'),
    import('three/addons/controls/OrbitControls.js')
  ]);
  THREE=threeModule;
  OBJLoader=objModule.OBJLoader;
  OrbitControls=controlsModule.OrbitControls;
}

function loadTexture(loader,url,isColor=false){
  return new Promise((resolve,reject)=>loader.load(url,texture=>{
    if(isColor)texture.colorSpace=THREE.SRGBColorSpace;
    texture.anisotropy=16;
    resolve(texture);
  },undefined,reject));
}

function loadObj(loader,url){
  return new Promise((resolve,reject)=>loader.load(url,resolve,undefined,reject));
}

async function initModelViewer(item){
  const host=$('#modelViewport');
  const status=$('#modelStatus');
  if(!host||!item.model)return;

  try{
    await ensureThree();
  }catch(error){
    console.error('Three.js failed to load',error);
    if(status){
      status.textContent='3D library could not load. Check your internet connection, then refresh.';
      status.classList.add('error');
    }
    return;
  }
  if(!document.body.contains(host))return;

  const scene=new THREE.Scene();
  scene.background=new THREE.Color(0xd5d7d4);
  const camera=new THREE.PerspectiveCamera(32,1,.01,100);
  camera.position.set(3.3,2.15,4.25);
  const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});
  renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,2));
  renderer.outputColorSpace=THREE.SRGBColorSpace;
  renderer.toneMapping=THREE.ACESFilmicToneMapping;
  renderer.toneMappingExposure=1.04;
  renderer.shadowMap.enabled=true;
  host.appendChild(renderer.domElement);

  const controls=new OrbitControls(camera,renderer.domElement);
  controls.enableDamping=true;
  controls.dampingFactor=.055;
  controls.enablePan=false;
  controls.autoRotate=true;
  controls.autoRotateSpeed=.56;
  controls.minDistance=1.7;
  controls.maxDistance=7.5;

  scene.add(new THREE.HemisphereLight(0xffffff,0x777a78,2.55));
  const key=new THREE.DirectionalLight(0xffffff,3.7);
  key.position.set(4.5,6,5);
  key.castShadow=true;
  scene.add(key);
  const rim=new THREE.DirectionalLight(0xc8d6e6,2.0);
  rim.position.set(-5,2,-4);
  scene.add(rim);
  const fill=new THREE.DirectionalLight(0xfff5e6,1.2);
  fill.position.set(0,-2,4);
  scene.add(fill);

  const floor=new THREE.Mesh(new THREE.CircleGeometry(3.4,96),new THREE.ShadowMaterial({opacity:.16}));
  floor.rotation.x=-Math.PI/2;
  floor.position.y=-1.28;
  floor.receiveShadow=true;
  scene.add(floor);

  const manager=new THREE.LoadingManager();
  manager.onProgress=(url,loaded,total)=>{
    if(status)status.textContent=`Loading textured object… ${Math.round(loaded/total*100)}%`;
  };
  const textureLoader=new THREE.TextureLoader(manager);
  const objLoader=new OBJLoader(manager);

  let animationId=null;
  const resize=()=>{
    const width=Math.max(1,host.clientWidth);
    const height=Math.max(1,host.clientHeight);
    renderer.setSize(width,height,false);
    camera.aspect=width/height;
    camera.updateProjectionMatrix();
  };
  const resizeObserver=new ResizeObserver(resize);
  resizeObserver.observe(host);
  resize();

  modelViewer={scene,camera,renderer,controls,resizeObserver,get animationId(){return animationId},set animationId(value){animationId=value}};

  try{
    const [object,diffuse,normal,ao]=await Promise.all([
      loadObj(objLoader,item.model.obj),
      loadTexture(textureLoader,item.model.diffuse,true),
      loadTexture(textureLoader,item.model.normal),
      loadTexture(textureLoader,item.model.ao)
    ]);
    if(!modelViewer||modelViewer.renderer!==renderer){
      object.traverse(child=>child.geometry?.dispose?.());
      return;
    }

    const material=new THREE.MeshStandardMaterial({
      map:diffuse,
      normalMap:normal,
      aoMap:ao,
      roughness:.88,
      metalness:.02,
      side:THREE.DoubleSide
    });
    material.normalScale.set(.72,.72);
    object.traverse(child=>{
      if(!child.isMesh)return;
      const uv=child.geometry.attributes.uv;
      if(uv&&!child.geometry.attributes.uv1)child.geometry.setAttribute('uv1',uv.clone());
      if(uv&&!child.geometry.attributes.uv2)child.geometry.setAttribute('uv2',uv.clone());
      child.material=material;
      child.castShadow=true;
      child.receiveShadow=true;
    });

    const [rx,ry,rz]=item.model.rotation||[0,0,0];
    object.rotation.set(rx,ry,rz);
    object.updateMatrixWorld(true);

    // Normalize, then center after scaling. Centering after the scale prevents
    // large source-coordinate offsets from making the object look tiny.
    const sourceBox=new THREE.Box3().setFromObject(object);
    const sourceSize=sourceBox.getSize(new THREE.Vector3());
    const largestDimension=Math.max(sourceSize.x,sourceSize.y,sourceSize.z)||1;
    object.scale.setScalar(3.55/largestDimension);
    object.updateMatrixWorld(true);
    const scaledBox=new THREE.Box3().setFromObject(object);
    const scaledCenter=scaledBox.getCenter(new THREE.Vector3());
    object.position.sub(scaledCenter);
    object.updateMatrixWorld(true);
    scene.add(object);

    // Fit the camera tightly to each object, regardless of whether the scan is
    // tall, flat, or unusually wide. The 1.07 margin keeps it large but unclipped.
    const fittedBox=new THREE.Box3().setFromObject(object);
    const sphere=fittedBox.getBoundingSphere(new THREE.Sphere());
    const verticalFov=THREE.MathUtils.degToRad(camera.fov);
    const horizontalFov=2*Math.atan(Math.tan(verticalFov/2)*camera.aspect);
    const verticalDistance=sphere.radius/Math.tan(verticalFov/2);
    const horizontalDistance=sphere.radius/Math.tan(horizontalFov/2);
    const cameraDistance=Math.max(verticalDistance,horizontalDistance)*1.07;
    const cameraDirection=new THREE.Vector3(3.3,2.15,4.25).normalize();
    camera.position.copy(cameraDirection.multiplyScalar(cameraDistance));
    camera.near=Math.max(.01,cameraDistance/100);
    camera.far=Math.max(100,cameraDistance*100);
    camera.updateProjectionMatrix();
    controls.minDistance=Math.max(.35,cameraDistance*.48);
    controls.maxDistance=Math.max(8,cameraDistance*3.2);
    floor.position.y=fittedBox.min.y-.045;
    controls.target.set(0,0,0);
    controls.update();
    if(status){
      status.textContent='Textured OBJ loaded';
      status.classList.add('loaded');
      setTimeout(()=>status?.remove(),1000);
    }
  }catch(error){
    console.error('3D model failed to load',error);
    if(status){
      status.textContent=`${item.title} could not load. Run the complete folder with Live Server and keep its assets directory intact.`;
      status.classList.add('error');
    }
  }

  const render=()=>{
    controls.update();
    renderer.render(scene,camera);
    animationId=requestAnimationFrame(render);
  };
  render();
}

function destroyModelViewer(){
  if(!modelViewer)return;
  cancelAnimationFrame(modelViewer.animationId);
  modelViewer.resizeObserver?.disconnect();
  modelViewer.controls?.dispose();
  modelViewer.scene?.traverse(object=>{
    object.geometry?.dispose?.();
    const materials=Array.isArray(object.material)?object.material:[object.material];
    materials.filter(Boolean).forEach(material=>{
      ['map','normalMap','aoMap','roughnessMap','metalnessMap'].forEach(key=>material[key]?.dispose?.());
      material.dispose?.();
    });
  });
  modelViewer.renderer?.dispose();
  modelViewer.renderer?.domElement?.remove();
  modelViewer=null;
}

$('#closeDetail').onclick=()=>closeDetail(true);

const canvas=$('#canvas');
canvas.addEventListener('pointerdown',event=>{
  if(event.target.closest('.node'))return;
  state.dragging=true;
  state.lastX=event.clientX;
  state.lastY=event.clientY;
  canvas.classList.add('dragging');
  canvas.setPointerCapture(event.pointerId);
});
canvas.addEventListener('pointermove',event=>{
  if(!state.dragging)return;
  state.x+=event.clientX-state.lastX;
  state.y+=event.clientY-state.lastY;
  state.lastX=event.clientX;
  state.lastY=event.clientY;
  applyTransform();
});
canvas.addEventListener('pointerup',()=>{
  state.dragging=false;
  canvas.classList.remove('dragging');
});
canvas.addEventListener('pointercancel',()=>{
  state.dragging=false;
  canvas.classList.remove('dragging');
});
canvas.addEventListener('wheel',event=>{
  event.preventDefault();
  const old=state.scale;
  state.scale=Math.min(1.65,Math.max(.38,state.scale*(event.deltaY<0?1.08:.92)));
  const rect=canvas.getBoundingClientRect();
  const mouseX=event.clientX-rect.left-rect.width/2;
  const mouseY=event.clientY-rect.top-rect.height/2;
  state.x=mouseX-(mouseX-state.x)*(state.scale/old);
  state.y=mouseY-(mouseY-state.y)*(state.scale/old);
  applyTransform();
},{passive:false});

$('#zoomIn').onclick=()=>{state.scale=Math.min(1.65,state.scale*1.15);applyTransform();};
$('#zoomOut').onclick=()=>{state.scale=Math.max(.38,state.scale/1.15);applyTransform();};
$('#zoomReset').onclick=()=>{
  state.scale=window.innerWidth<760?.68:.86;
  state.x=0;
  state.y=0;
  applyTransform();
};

function renderFilters(){
  const categories=['Material','Fabrication','Typology','Geometry','Project Phase'];
  $('#relationshipFilters').innerHTML=categories.map(category=>`<label class="relationship-filter-option"><input type="checkbox" checked value="${category}"><span>${category}</span></label>`).join('');
  $$('#relationshipFilters input').forEach(input=>input.onchange=()=>{
    input.checked?state.filters.add(input.value):state.filters.delete(input.value);
    updateRelationshipNetworkVisibility();
  });
  $$('.relationship-mode').forEach(button=>button.onclick=()=>{
    relationshipState.mode=button.dataset.relationshipMode;
    $$('.relationship-mode').forEach(entry=>entry.classList.toggle('active',entry===button));
    updateRelationshipNetworkVisibility();
  });
  $('#relationshipResetView')?.addEventListener('click',resetRelationshipCamera);
  $('#openRelationshipPrototype')?.addEventListener('click',()=>{
    if(relationshipState.selectedId)focusItem(relationshipState.selectedId);
  });
}

function renderRelationships(){
  updateRelationshipPanels(relationshipState.selectedId);
  updateRelationshipNetworkVisibility();
  if(state.view==='relationships')requestAnimationFrame(()=>initRelationshipNetwork());
}

function relationshipFamily(item){
  if(item.id.includes('-3dp-'))return '3D Print';
  if(item.id.includes('-col-'))return 'Column Print';
  if(item.id.includes('-con-'))return 'Concrete Print';
  if(item.id.includes('-win-'))return 'Window Profile';
  return 'Large Scale';
}

function relationshipCategoryColor(category){
  return {
    'Material':0x9ba6b3,
    'Typology':0xc8cdcf,
    'Fabrication':0xaeb2b0,
    'Geometry':0xe3ded3,
    'Project Phase':0x717981
  }[category]||0xaeb2b0;
}

function relationshipLayout(){
  const definitions={
    '3D Print':{center:[-6.4,4.15,-2.8],direction:[1.55,.16,.72]},
    'Column Print':{center:[-1.45,4.75,.45],direction:[1.42,-.05,-.7]},
    'Concrete Print':{center:[4.45,3.8,.95],direction:[1.5,.14,.72]},
    'Window Profile':{center:[6.5,5.25,-2.35],direction:[1.45,-.08,.9]},
    'Large Scale':{center:[.1,6.0,3.1],direction:[1,0,0]}
  };
  const groups=new Map();
  archiveItems.forEach(item=>{
    const family=relationshipFamily(item);
    if(!groups.has(family))groups.set(family,[]);
    groups.get(family).push(item);
  });
  const positions=new Map();
  groups.forEach((items,family)=>{
    const definition=definitions[family];
    items.forEach((item,index)=>{
      const offset=index-(items.length-1)/2;
      positions.set(item.id,new THREE.Vector3(
        definition.center[0]+definition.direction[0]*offset,
        definition.center[1]+definition.direction[1]*offset+Math.sin(index*1.47+family.length)*.36,
        definition.center[2]+definition.direction[2]*offset+Math.cos(index*1.19+family.length)*.48
      ));
    });
  });
  return positions;
}

function makeRelationshipLabel(text,subtext=''){
  const canvas=document.createElement('canvas');
  canvas.width=640;canvas.height=150;
  const context=canvas.getContext('2d');
  context.clearRect(0,0,canvas.width,canvas.height);
  context.font='600 34px Arial';
  context.fillStyle='rgba(236,238,237,.96)';
  context.fillText(text.slice(0,31),18,48);
  context.font='24px Arial';
  context.fillStyle='rgba(158,164,167,.92)';
  context.fillText(subtext.slice(0,42),18,86);
  const texture=new THREE.CanvasTexture(canvas);
  texture.colorSpace=THREE.SRGBColorSpace;
  texture.minFilter=THREE.LinearFilter;
  const material=new THREE.SpriteMaterial({map:texture,transparent:true,depthWrite:false,opacity:.88});
  const sprite=new THREE.Sprite(material);
  sprite.scale.set(4.25,.99,1);
  sprite.userData.labelTexture=texture;
  return sprite;
}

function makeNetworkNode(item,position,index){
  const group=new THREE.Group();
  const restPosition=position.clone();
  const dropHeight=4.6+(index%4)*.52;
  group.position.copy(restPosition);
  if(!prefersReducedMotion){
    group.position.x+=Math.sin(index*1.31)*.18;
    group.position.y+=dropHeight;
    group.position.z+=Math.cos(index*1.17)*.14;
  }
  group.userData.itemId=item.id;

  const supportHeight=4.8+(index%5)*.42;
  const supportGeometry=new THREE.BufferGeometry().setFromPoints([
    new THREE.Vector3(0,.22,0),
    new THREE.Vector3(0,supportHeight,0)
  ]);
  const supportMaterial=new THREE.LineBasicMaterial({
    color:0xa5a6a0,
    transparent:true,
    opacity:.2,
    depthWrite:false
  });
  const support=new THREE.Line(supportGeometry,supportMaterial);
  group.add(support);

  const weightMaterial=new THREE.MeshStandardMaterial({
    color:index%4===0?0xc3b89f:0xa8aaa5,
    roughness:.82,
    metalness:.02,
    emissive:0x111311,
    transparent:true,
    opacity:.95
  });
  const mesh=new THREE.Mesh(new THREE.SphereGeometry(.29,18,14),weightMaterial);
  mesh.scale.set(.72,1.28,.72);
  mesh.userData.itemId=item.id;
  group.add(mesh);

  const cap=new THREE.Mesh(
    new THREE.ConeGeometry(.12,.28,10),
    new THREE.MeshStandardMaterial({color:0x777872,roughness:.86,metalness:.02,transparent:true,opacity:.9})
  );
  cap.position.y=.47;
  cap.userData.itemId=item.id;
  group.add(cap);

  const edgeMaterial=new THREE.MeshBasicMaterial({color:0xedece5,transparent:true,opacity:.38,depthWrite:false});
  const edges=new THREE.Mesh(new THREE.TorusGeometry(.37,.012,6,36),edgeMaterial);
  edges.rotation.x=Math.PI/2;
  edges.userData.itemId=item.id;
  group.add(edges);

  const marker=new THREE.Mesh(
    new THREE.SphereGeometry(.065,10,8),
    new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.9,depthWrite:false})
  );
  marker.position.set(0,-.31,.02);
  marker.userData.itemId=item.id;
  group.add(marker);

  const label=makeRelationshipLabel(item.archiveCode,`${relationshipFamily(item)} · ${item.year}`);
  label.position.set(1.82,-.03,.04);
  label.scale.multiplyScalar(.9);
  label.userData.itemId=item.id;
  group.add(label);

  group.userData={
    itemId:item.id,
    mesh,
    edges,
    marker,
    label,
    support,
    cap,
    supportHeight,
    restPosition,
    dropVelocity:new THREE.Vector3(
      Math.sin(index*1.73)*.08,
      -.24-(index%3)*.035,
      Math.cos(index*1.41)*.065
    ),
    dropDelay:index*.065,
    baseScale:1,
    swayPhase:index*.73
  };
  return group;
}

function simulateRelationshipNodeDrop(node,dt,elapsed,inertialForce){
  const data=node?.userData;
  const rest=data?.restPosition;
  const velocity=data?.dropVelocity;
  if(!node||!rest||!velocity)return;

  const supportAttribute=data.support?.geometry?.getAttribute?.('position');
  const updateSupport=()=>{
    if(!supportAttribute)return;
    const array=supportAttribute.array;
    array[0]=0;array[1]=.22;array[2]=0;
    array[3]=rest.x-node.position.x;
    array[4]=rest.y+finiteNumber(data.supportHeight,5)-node.position.y;
    array[5]=rest.z-node.position.z;
    supportAttribute.needsUpdate=true;
  };

  if(prefersReducedMotion){
    node.position.copy(rest);
    velocity.set(0,0,0);
    updateSupport();
    return;
  }

  const delay=finiteNumber(data.dropDelay,0);
  if(elapsed<delay){
    updateSupport();
    return;
  }

  const step=Math.min(Math.max(finiteNumber(dt,0),0),.033);
  const spring=22.5;
  const damping=5.15;
  const forceX=finiteNumber(inertialForce?.x)*.34;
  const forceY=finiteNumber(inertialForce?.y)*.08;
  const forceZ=finiteNumber(inertialForce?.z)*.34;

  velocity.x+=((rest.x-node.position.x)*spring+forceX)*step;
  velocity.y+=((rest.y-node.position.y)*spring+forceY)*step;
  velocity.z+=((rest.z-node.position.z)*spring+forceZ)*step;
  velocity.multiplyScalar(Math.exp(-damping*step));
  node.position.addScaledVector(velocity,step);

  const nearRest=node.position.distanceToSquared(rest)<.000025;
  const nearlyStill=velocity.lengthSq()<.00009;
  const fieldQuiet=(inertialForce?.lengthSq?.()||0)<.0025;
  if(nearRest&&nearlyStill&&fieldQuiet){
    node.position.copy(rest);
    velocity.set(0,0,0);
  }
  updateSupport();
}

function makeHangingConnection(relationship,source,target,index){
  const distance=Math.max(.001,source.distanceTo(target));
  const segmentCount=Math.max(22,Math.min(42,Math.round(distance*2.65)+13));
  const extraLength=.14+(1-relationship.strength)*.07+(index%4)*.008;
  const restLength=distance*(1+extraLength)/segmentCount;
  const direction=target.clone().sub(source);
  const sideways=new THREE.Vector3().crossVectors(direction,new THREE.Vector3(0,1,0));
  if(sideways.lengthSq()<.001)sideways.set(0,0,1);
  sideways.normalize();
  const positions=[];
  const previous=[];
  const entryLift=1.1+distance*.065+(index%3)*.24;
  const sideOffset=((index%2===0)?1:-1)*(.12+(index%5)*.035);

  for(let i=0;i<=segmentCount;i++){
    const t=i/segmentCount;
    const point=source.clone().lerp(target,t);
    if(prefersReducedMotion){
      point.y-=Math.sin(Math.PI*t)*(distance*.17+1.1);
    }else{
      point.y+=Math.sin(Math.PI*t)*entryLift;
      point.addScaledVector(sideways,Math.sin(Math.PI*t)*sideOffset);
    }
    positions.push(point);
    const oldPoint=point.clone();
    if(!prefersReducedMotion&&i>0&&i<segmentCount){
      oldPoint.y+=.16+Math.sin(i*.81+index)*.055;
      oldPoint.addScaledVector(sideways,Math.cos(i*.47+index)*.025);
    }
    previous.push(oldPoint);
  }

  positions[0].copy(source);
  positions[segmentCount].copy(target);
  previous[0].copy(source);
  previous[segmentCount].copy(target);

  const positionArray=new Float32Array((segmentCount+1)*3);
  const geometry=new THREE.BufferGeometry();
  const attribute=new THREE.BufferAttribute(positionArray,3);
  attribute.setUsage(THREE.DynamicDrawUsage);
  geometry.setAttribute('position',attribute);

  const categoryColor=relationshipCategoryColor(relationship.category);
  const haloMaterial=new THREE.LineBasicMaterial({
    color:categoryColor,
    transparent:true,
    opacity:.08,
    depthWrite:false,
    blending:THREE.AdditiveBlending
  });
  const lineMaterial=new THREE.LineBasicMaterial({
    color:categoryColor,
    transparent:true,
    opacity:.34,
    depthWrite:false
  });
  const halo=new THREE.Line(geometry,haloMaterial);
  const line=new THREE.Line(geometry,lineMaterial);
  halo.frustumCulled=false;
  line.frustumCulled=false;

  const beadIndices=[];
  for(let i=2;i<segmentCount;i+=4)beadIndices.push(i);
  const beadMaterial=new THREE.MeshBasicMaterial({color:categoryColor,transparent:true,opacity:.38,depthWrite:false});
  const beads=new THREE.InstancedMesh(new THREE.SphereGeometry(.045,7,6),beadMaterial,beadIndices.length);
  beads.instanceMatrix.setUsage(THREE.DynamicDrawUsage);
  beads.frustumCulled=false;

  const signal=new THREE.Mesh(
    new THREE.SphereGeometry(.075+relationship.strength*.025,10,8),
    new THREE.MeshBasicMaterial({color:categoryColor,transparent:true,opacity:.8,depthWrite:false})
  );
  const label=makeRelationshipLabel(relationship.category,`${Math.round(relationship.strength*100)}% relationship`);
  label.scale.multiplyScalar(.52);
  label.material.opacity=.38;
  label.visible=relationship.strength>=.72;

  return {
    relationship,
    sourcePosition:source,
    targetPosition:target,
    positions,
    previous,
    segmentCount,
    restLength,
    sideways,
    geometry,
    attribute,
    halo,
    line,
    beads,
    beadIndices,
    signal,
    label,
    phase:(index*.137)%1,
    seed:index*1.713,
    direct:false
  };
}

const ropeDelta={x:0,y:0,z:0};
const ropeMatrixHolder={matrix:null};

function finiteNumber(value,fallback=0){
  const number=Number(value);
  return Number.isFinite(number)?number:fallback;
}

function safeConnectionPoint(connection,index){
  const positions=connection?.positions;
  if(!Array.isArray(positions)||positions.length===0)return null;
  const clamped=Math.max(0,Math.min(positions.length-1,Math.floor(finiteNumber(index,0))));
  const point=positions[clamped];
  if(point&&Number.isFinite(point.x)&&Number.isFinite(point.y)&&Number.isFinite(point.z))return point;

  // Repair a missing/corrupt rope point from the nearest valid neighbors.
  let before=null,after=null;
  for(let i=clamped-1;i>=0;i--){
    const candidate=positions[i];
    if(candidate&&Number.isFinite(candidate.x)&&Number.isFinite(candidate.y)&&Number.isFinite(candidate.z)){before=candidate;break;}
  }
  for(let i=clamped+1;i<positions.length;i++){
    const candidate=positions[i];
    if(candidate&&Number.isFinite(candidate.x)&&Number.isFinite(candidate.y)&&Number.isFinite(candidate.z)){after=candidate;break;}
  }
  if(before&&after){
    const repaired=new THREE.Vector3(
      (before.x+after.x)*.5,
      (before.y+after.y)*.5,
      (before.z+after.z)*.5
    );
    positions[clamped]=repaired;
    return repaired;
  }
  const fallback=before||after||connection?.sourcePosition||connection?.targetPosition;
  if(fallback){
    const repaired=new THREE.Vector3(finiteNumber(fallback.x),finiteNumber(fallback.y),finiteNumber(fallback.z));
    positions[clamped]=repaired;
    return repaired;
  }
  return null;
}

function updateHangingConnectionGeometry(connection,elapsed=0){
  if(!connection||!Array.isArray(connection.positions)||connection.positions.length<2)return false;

  const positions=connection.positions;
  const lastAvailable=positions.length-1;
  const declaredSegments=Math.floor(finiteNumber(connection.segmentCount,lastAvailable));
  const segmentCount=Math.max(1,Math.min(declaredSegments,lastAvailable));
  connection.segmentCount=segmentCount;

  const array=connection.attribute?.array;
  if(array){
    for(let index=0;index<positions.length;index++){
      const point=safeConnectionPoint(connection,index);
      const base=index*3;
      array[base]=finiteNumber(point?.x);
      array[base+1]=finiteNumber(point?.y);
      array[base+2]=finiteNumber(point?.z);
    }
    connection.attribute.needsUpdate=true;
  }

  if(!ropeMatrixHolder.matrix)ropeMatrixHolder.matrix=new THREE.Matrix4();
  if(connection.beads&&Array.isArray(connection.beadIndices)){
    connection.beadIndices.forEach((pointIndex,instanceIndex)=>{
      const point=safeConnectionPoint(connection,pointIndex);
      if(!point)return;
      ropeMatrixHolder.matrix.makeTranslation(
        finiteNumber(point.x),finiteNumber(point.y),finiteNumber(point.z)
      );
      connection.beads.setMatrixAt(instanceIndex,ropeMatrixHolder.matrix);
    });
    if(connection.beads.instanceMatrix)connection.beads.instanceMatrix.needsUpdate=true;
  }

  // Intentionally avoid Vector3.copy() here. Direct interpolation cannot receive
  // an undefined source and therefore cannot crash the whole render loop.
  const strength=Math.max(0,Math.min(1,finiteNumber(connection.relationship?.strength,.5)));
  const phase=finiteNumber(connection.phase,0);
  const time=finiteNumber(elapsed,0);
  const signalT=((time*(.045+strength*.035)+phase)%1+1)%1;
  const scaled=Math.max(0,Math.min(segmentCount-.000001,signalT*segmentCount));
  const low=Math.max(0,Math.min(segmentCount-1,Math.floor(scaled)));
  const high=Math.min(segmentCount,low+1);
  const from=safeConnectionPoint(connection,low);
  const to=safeConnectionPoint(connection,high)||from;
  const alpha=Math.max(0,Math.min(1,scaled-low));
  if(connection.signal&&from&&to){
    connection.signal.position.set(
      finiteNumber(from.x)+(finiteNumber(to.x)-finiteNumber(from.x))*alpha,
      finiteNumber(from.y)+(finiteNumber(to.y)-finiteNumber(from.y))*alpha,
      finiteNumber(from.z)+(finiteNumber(to.z)-finiteNumber(from.z))*alpha
    );
  }

  const midpoint=safeConnectionPoint(connection,Math.floor(segmentCount*.5));
  if(connection.label&&midpoint){
    connection.label.position.set(
      finiteNumber(midpoint.x),
      finiteNumber(midpoint.y)+.34,
      finiteNumber(midpoint.z)
    );
  }
  return true;
}

function simulateHangingConnection(connection,dt,gravity,elapsed){
  if(!connection||!Array.isArray(connection.positions)||!Array.isArray(connection.previous))return;
  const points=connection.positions;
  const previous=connection.previous;
  const requestedLast=Math.floor(finiteNumber(connection.segmentCount,points.length-1));
  const last=Math.max(1,Math.min(requestedLast,points.length-1,previous.length-1));
  connection.segmentCount=last;

  // Repair every point before the physics pass so malformed data cannot spread NaNs.
  for(let i=0;i<=last;i++){
    const point=safeConnectionPoint(connection,i);
    if(!point)return;
    if(!previous[i]||!Number.isFinite(previous[i].x)||!Number.isFinite(previous[i].y)||!Number.isFinite(previous[i].z)){
      previous[i]=point.clone();
    }
  }

  if(prefersReducedMotion){
    updateHangingConnectionGeometry(connection,elapsed);
    return;
  }

  const substeps=2;
  const step=Math.min(Math.max(finiteNumber(dt,0),0),.033)/substeps;
  const stepSquared=step*step;
  const damping=.986;
  const gravityX=finiteNumber(gravity?.x);
  const gravityY=finiteNumber(gravity?.y,-7.4);
  const gravityZ=finiteNumber(gravity?.z);
  const sidewaysX=finiteNumber(connection.sideways?.x);
  const sidewaysZ=finiteNumber(connection.sideways?.z);
  const seed=finiteNumber(connection.seed);

  for(let substep=0;substep<substeps;substep++){
    for(let i=1;i<last;i++){
      const point=points[i];
      const old=previous[i];
      const velocityX=(point.x-old.x)*damping;
      const velocityY=(point.y-old.y)*damping;
      const velocityZ=(point.z-old.z)*damping;
      old.set(point.x,point.y,point.z);
      const flutter=Math.sin(finiteNumber(elapsed)*.82+i*.57+seed)*.12;
      point.x+=velocityX+(gravityX+sidewaysX*flutter)*stepSquared;
      point.y+=velocityY+gravityY*stepSquared;
      point.z+=velocityZ+(gravityZ+sidewaysZ*flutter)*stepSquared;
    }

    for(let iteration=0;iteration<6;iteration++){
      const source=connection.sourcePosition;
      const target=connection.targetPosition;
      if(source)points[0].set(finiteNumber(source.x),finiteNumber(source.y),finiteNumber(source.z));
      if(target)points[last].set(finiteNumber(target.x),finiteNumber(target.y),finiteNumber(target.z));

      for(let i=0;i<last;i++){
        const a=points[i];
        const b=points[i+1];
        if(!a||!b)continue;
        ropeDelta.x=b.x-a.x;
        ropeDelta.y=b.y-a.y;
        ropeDelta.z=b.z-a.z;
        const distance=Math.hypot(ropeDelta.x,ropeDelta.y,ropeDelta.z)||.0001;
        const restLength=Math.max(.0001,finiteNumber(connection.restLength,distance));
        const correction=(distance-restLength)/distance;
        if(i===0){
          b.x-=ropeDelta.x*correction;
          b.y-=ropeDelta.y*correction;
          b.z-=ropeDelta.z*correction;
        }else if(i+1===last){
          a.x+=ropeDelta.x*correction;
          a.y+=ropeDelta.y*correction;
          a.z+=ropeDelta.z*correction;
        }else{
          const half=correction*.5;
          a.x+=ropeDelta.x*half;
          a.y+=ropeDelta.y*half;
          a.z+=ropeDelta.z*half;
          b.x-=ropeDelta.x*half;
          b.y-=ropeDelta.y*half;
          b.z-=ropeDelta.z*half;
        }
      }

      for(let i=2;i<last-1;i++){
        const before=points[i-1],point=points[i],after=points[i+1];
        if(!before||!point||!after)continue;
        point.x+=(((before.x+after.x)*.5)-point.x)*.012;
        point.y+=(((before.y+after.y)*.5)-point.y)*.012;
        point.z+=(((before.z+after.z)*.5)-point.z)*.012;
      }
    }
  }
  updateHangingConnectionGeometry(connection,elapsed);
}

function updateRelationshipPanels(id){
  const item=archiveItems.find(entry=>entry.id===id)||archiveItems[0];
  if(!item)return;
  relationshipState.selectedId=item.id;
  const connected=relationships
    .filter(relationship=>relationship.source===item.id||relationship.target===item.id)
    .sort((a,b)=>b.strength-a.strength);
  const strongest=connected[0];
  const neighbor=strongest?archiveItems.find(entry=>entry.id===(strongest.source===item.id?strongest.target:strongest.source)):null;

  const code=$('#relationshipSelectedCode');if(code)code.textContent=item.archiveCode;
  const title=$('#relationshipSelectedTitle');if(title)title.textContent=item.title;
  const score=$('#relationshipDetailScore');if(score)score.textContent=strongest?`${Math.round(strongest.strength*100)}% similarity`:'No direct match';
  const category=$('#relationshipDetailCategory');if(category)category.textContent=strongest?`${strongest.category} · ${neighbor?.title||''}`:'No relationship selected';
  const detail=$('#relationshipDetailText');if(detail)detail.textContent=strongest?strongest.description:'This prototype currently has no explicit connections in the archive dataset.';

  const list=$('#relationshipNeighborList');
  if(list)list.innerHTML=connected.slice(0,4).map(relationship=>{
    const other=archiveItems.find(entry=>entry.id===(relationship.source===item.id?relationship.target:relationship.source));
    return `<button class="relationship-neighbor" data-neighbor-id="${other.id}"><b>${other.title}</b><span>${Math.round(relationship.strength*100)}%</span></button>`;
  }).join('');
  $$('.relationship-neighbor').forEach(button=>button.onclick=()=>selectRelationshipNode(button.dataset.neighborId,true));

  const preview=$('#relationshipPreviewImage');
  const fallback=$('#relationshipPreviewFallback');
  if(preview){
    if(fallback)fallback.style.display='';
    preview.classList.remove('visible');
    preview.removeAttribute('src');
    if(item.model?.diffuse){
      preview.onload=()=>{preview.classList.add('visible');if(fallback)fallback.style.display='none';};
      preview.onerror=()=>{preview.classList.remove('visible');if(fallback)fallback.style.display='';};
      preview.src=item.model.diffuse;
    }else if(fallback)fallback.style.display='';
  }
  const openButton=$('#openRelationshipPrototype');if(openButton)openButton.disabled=!item;
}

function selectRelationshipNode(id,focus=false){
  relationshipState.selectedId=id;
  updateRelationshipPanels(id);
  updateRelationshipNetworkVisibility();
  if(focus&&relationshipViewer){
    const node=relationshipViewer.nodes.get(id);
    if(node)relationshipViewer.focusTarget=node.position.clone();
  }
}

function visibleRelationshipSet(){
  return relationships.filter(relationship=>{
    if(!state.filters.has(relationship.category))return false;
    if(relationshipState.mode==='direct')return relationship.source===relationshipState.selectedId||relationship.target===relationshipState.selectedId;
    if(relationshipState.mode==='strongest')return relationship.strength>=.65;
    return true;
  });
}

function updateRelationshipNetworkVisibility(){
  const visible=visibleRelationshipSet();
  const visibleKeys=new Set(visible.map(relationship=>`${relationship.source}|${relationship.target}|${relationship.category}`));
  const involved=new Set();
  visible.forEach(relationship=>{involved.add(relationship.source);involved.add(relationship.target);});
  $('#relationshipVisibleCount')&&( $('#relationshipVisibleCount').textContent=String(visible.length).padStart(3,'0') );
  if(!relationshipViewer)return;

  relationshipViewer.connections.forEach(connection=>{
    const key=`${connection.relationship.source}|${connection.relationship.target}|${connection.relationship.category}`;
    const shown=visibleKeys.has(key);
    const direct=connection.relationship.source===relationshipState.selectedId||connection.relationship.target===relationshipState.selectedId;
    connection.direct=shown&&direct;
    connection.line.visible=shown;
    connection.halo.visible=shown;
    connection.beads.visible=shown;
    connection.signal.visible=shown&&direct;
    connection.label.visible=shown&&(direct||connection.relationship.strength>=.75);
    if(shown){
      const color=direct?0xf3f1e8:relationshipCategoryColor(connection.relationship.category);
      connection.line.material.color.setHex(color);
      connection.halo.material.color.setHex(color);
      connection.beads.material.color.setHex(color);
      connection.signal.material.color.setHex(color);
      connection.line.material.opacity=direct?.96:.2+connection.relationship.strength*.22;
      connection.halo.material.opacity=direct?.23:.045;
      connection.beads.material.opacity=direct?.8:.2;
      connection.signal.material.opacity=direct?1:.65;
      connection.label.material.opacity=direct?.94:.36;
    }
  });

  const connectedToSelected=new Set();
  visible.forEach(relationship=>{
    if(relationship.source===relationshipState.selectedId)connectedToSelected.add(relationship.target);
    if(relationship.target===relationshipState.selectedId)connectedToSelected.add(relationship.source);
  });

  relationshipViewer.nodes.forEach((group,id)=>{
    const selected=id===relationshipState.selectedId;
    const connected=connectedToSelected.has(id);
    const active=relationshipState.mode==='all'||involved.has(id)||selected;
    group.visible=true;
    group.userData.mesh.material.opacity=selected?1:(connected?.96:(active?.78:.12));
    group.userData.mesh.material.emissive.setHex(selected?0x5d5546:(connected?0x282a27:0x111311));
    group.userData.edges.material.opacity=selected?1:(connected?.72:(active?.28:.06));
    group.userData.marker.material.opacity=selected?1:(connected?.92:(active?.58:.08));
    group.userData.label.material.opacity=selected?1:(connected?.9:(active?.58:.08));
    group.userData.support.material.opacity=selected?.52:(connected?.34:(active?.16:.04));
    group.userData.cap.material.opacity=selected?1:(active?.76:.1);
    group.scale.setScalar(selected?1.48:(connected?1.16:1));
  });
}

async function initRelationshipNetwork(){
  const host=$('#relationshipNetwork');
  const status=$('#relationshipNetworkStatus');
  if(!host||state.view!=='relationships')return;
  if(relationshipViewer?.host===host){
    relationshipViewer.resize();
    updateRelationshipNetworkVisibility();
    if(status){status.textContent='';status.classList.add('loaded');}
    return;
  }
  if(status){status.textContent='Releasing suspended relationship field…';status.classList.remove('loaded','error');}
  destroyRelationshipNetwork();
  try{await ensureThree();}
  catch(error){
    console.error('Relationship network failed to load',error);
    if(status){status.textContent='3D relationship library could not load. Check the connection and refresh.';status.classList.add('error');}
    return;
  }
  if(state.view!=='relationships'||!document.body.contains(host))return;

  const scene=new THREE.Scene();
  scene.background=new THREE.Color(0x080a09);
  scene.fog=new THREE.FogExp2(0x080a09,.023);
  const camera=new THREE.PerspectiveCamera(43,1,.1,160);
  camera.position.set(.7,7.1,22.8);
  const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});
  renderer.setPixelRatio(Math.min(window.devicePixelRatio,1.8));
  renderer.outputColorSpace=THREE.SRGBColorSpace;
  host.innerHTML='';host.appendChild(renderer.domElement);

  const controls=new OrbitControls(camera,renderer.domElement);
  controls.enableDamping=true;
  controls.dampingFactor=.06;
  controls.enablePan=true;
  controls.screenSpacePanning=true;
  controls.minDistance=7;
  controls.maxDistance=46;
  controls.target.set(.2,1.65,0);
  controls.update();

  scene.add(new THREE.HemisphereLight(0xd7d5ca,0x090b0a,1.45));
  const keyLight=new THREE.DirectionalLight(0xf4efe1,1.65);keyLight.position.set(5,12,10);scene.add(keyLight);
  const rimLight=new THREE.DirectionalLight(0x8190a1,.9);rimLight.position.set(-10,4,-8);scene.add(rimLight);

  const overheadGeometry=new THREE.BufferGeometry();
  const overheadPositions=[];
  for(let x=-11;x<=11;x+=2.2){
    overheadPositions.push(x,9.8,-6,x,9.8,6);
  }
  for(let z=-6;z<=6;z+=2){
    overheadPositions.push(-11,9.8,z,11,9.8,z);
  }
  overheadGeometry.setAttribute('position',new THREE.Float32BufferAttribute(overheadPositions,3));
  const overheadFrame=new THREE.LineSegments(overheadGeometry,new THREE.LineBasicMaterial({color:0x9b9a91,transparent:true,opacity:.075,depthWrite:false}));
  scene.add(overheadFrame);

  const positions=relationshipLayout();
  const nodeMap=new Map();
  const pickables=[];
  archiveItems.forEach((item,index)=>{
    const node=makeNetworkNode(item,positions.get(item.id),index);
    nodeMap.set(item.id,node);
    node.traverse(child=>{if(child.isMesh&&child.userData?.itemId)pickables.push(child);});
    scene.add(node);
  });

  const connections=[];
  relationships.forEach((relationship,index)=>{
    const sourceRest=positions.get(relationship.source);
    const targetRest=positions.get(relationship.target);
    const sourceNode=nodeMap.get(relationship.source);
    const targetNode=nodeMap.get(relationship.target);
    if(!sourceRest||!targetRest||!sourceNode||!targetNode)return;

    // Build the rope from the currently elevated weights, then normalize its
    // rest length to the final layout so it follows the falling nodes cleanly.
    const connection=makeHangingConnection(
      relationship,
      sourceNode.position,
      targetNode.position,
      index
    );
    const initialDistance=Math.max(.001,sourceNode.position.distanceTo(targetNode.position));
    const finalDistance=Math.max(.001,sourceRest.distanceTo(targetRest));
    connection.restLength*=finalDistance/initialDistance;
    connection.sourcePosition=sourceNode.position;
    connection.targetPosition=targetNode.position;
    updateHangingConnectionGeometry(connection,0);
    scene.add(connection.halo,connection.line,connection.beads,connection.signal,connection.label);
    connections.push(connection);
  });

  const dustGeometry=new THREE.BufferGeometry();
  const dustPositions=[];
  for(let i=0;i<420;i++)dustPositions.push((Math.random()-.5)*38,(Math.random()-.5)*24,(Math.random()-.5)*20);
  dustGeometry.setAttribute('position',new THREE.Float32BufferAttribute(dustPositions,3));
  const dust=new THREE.Points(dustGeometry,new THREE.PointsMaterial({color:0xb7b5aa,size:.016,transparent:true,opacity:.25,depthWrite:false}));
  scene.add(dust);

  const raycaster=new THREE.Raycaster();
  const pointer=new THREE.Vector2();
  const tooltip=$('#relationshipNetworkTooltip');
  const inertialForce=new THREE.Vector3();
  const rightVector=new THREE.Vector3();
  const upVector=new THREE.Vector3();
  const forwardVector=new THREE.Vector3();
  const pointerPhysics={active:false,x:0,y:0};

  const setPointer=event=>{
    const rect=renderer.domElement.getBoundingClientRect();
    pointer.x=((event.clientX-rect.left)/rect.width)*2-1;
    pointer.y=-((event.clientY-rect.top)/rect.height)*2+1;
  };
  const findItemId=object=>{
    let current=object;
    while(current){if(current.userData?.itemId)return current.userData.itemId;current=current.parent;}
    return null;
  };
  const disturbField=(dx,dy)=>{
    rightVector.set(1,0,0).applyQuaternion(camera.quaternion);
    upVector.set(0,1,0).applyQuaternion(camera.quaternion);
    inertialForce.addScaledVector(rightVector,-dx*.055);
    inertialForce.addScaledVector(upVector,dy*.042);
    if(inertialForce.length()>11)inertialForce.setLength(11);
  };
  const onPointerDown=event=>{
    pointerPhysics.active=true;
    pointerPhysics.x=event.clientX;
    pointerPhysics.y=event.clientY;
  };
  const onPointerUp=()=>{pointerPhysics.active=false;};
  const onPointerMove=event=>{
    if(pointerPhysics.active){
      const dx=event.clientX-pointerPhysics.x;
      const dy=event.clientY-pointerPhysics.y;
      pointerPhysics.x=event.clientX;
      pointerPhysics.y=event.clientY;
      disturbField(dx,dy);
    }
    setPointer(event);raycaster.setFromCamera(pointer,camera);
    const hit=raycaster.intersectObjects(pickables,false)[0];
    const id=hit?findItemId(hit.object):null;
    relationshipState.hoveredId=id;
    renderer.domElement.style.cursor=id?'pointer':'grab';
    if(tooltip){
      if(id){
        const item=archiveItems.find(entry=>entry.id===id);
        tooltip.innerHTML=`<strong>${item.archiveCode}</strong><br>${item.title}`;
        tooltip.style.left=`${event.clientX-renderer.domElement.getBoundingClientRect().left}px`;
        tooltip.style.top=`${event.clientY-renderer.domElement.getBoundingClientRect().top}px`;
        tooltip.classList.add('visible');tooltip.setAttribute('aria-hidden','false');
      }else{tooltip.classList.remove('visible');tooltip.setAttribute('aria-hidden','true');}
    }
  };
  const onWheel=event=>{
    camera.getWorldDirection(forwardVector);
    inertialForce.addScaledVector(forwardVector,Math.sign(event.deltaY)*1.35);
    if(inertialForce.length()>11)inertialForce.setLength(11);
  };
  const onClick=event=>{
    setPointer(event);raycaster.setFromCamera(pointer,camera);
    const hit=raycaster.intersectObjects(pickables,false)[0];
    const id=hit?findItemId(hit.object):null;
    if(id)selectRelationshipNode(id,false);
  };
  const onDoubleClick=event=>{
    setPointer(event);raycaster.setFromCamera(pointer,camera);
    const hit=raycaster.intersectObjects(pickables,false)[0];
    const id=hit?findItemId(hit.object):null;
    if(id)focusItem(id);
  };
  const onPointerLeave=()=>{
    pointerPhysics.active=false;
    tooltip?.classList.remove('visible');
  };
  renderer.domElement.addEventListener('pointerdown',onPointerDown);
  renderer.domElement.addEventListener('pointerup',onPointerUp);
  renderer.domElement.addEventListener('pointercancel',onPointerUp);
  renderer.domElement.addEventListener('pointermove',onPointerMove);
  renderer.domElement.addEventListener('pointerleave',onPointerLeave);
  renderer.domElement.addEventListener('wheel',onWheel,{passive:true});
  renderer.domElement.addEventListener('click',onClick);
  renderer.domElement.addEventListener('dblclick',onDoubleClick);

  const resize=()=>{
    const width=Math.max(1,host.clientWidth),height=Math.max(1,host.clientHeight);
    renderer.setSize(width,height,false);camera.aspect=width/height;camera.updateProjectionMatrix();
  };
  const resizeObserver=new ResizeObserver(resize);resizeObserver.observe(host);resize();

  relationshipViewer={
    host,scene,camera,renderer,controls,nodes:nodeMap,connections,resizeObserver,
    resize,animationId:null,elapsed:0,focusTarget:null,inertialForce,
    listeners:{onPointerDown,onPointerUp,onPointerMove,onPointerLeave,onWheel,onClick,onDoubleClick}
  };
  updateRelationshipPanels(relationshipState.selectedId);
  updateRelationshipNetworkVisibility();
  if(status){setTimeout(()=>{if(status){status.classList.add('loaded');status.textContent='';}},1250);}

  let last=performance.now();
  const gravity=new THREE.Vector3();
  const animate=now=>{
    if(!relationshipViewer||relationshipViewer.renderer!==renderer)return;
    const delta=Math.min((now-last)/1000,.05);last=now;
    relationshipViewer.elapsed+=delta;

    inertialForce.multiplyScalar(Math.exp(-delta*1.9));
    gravity.set(inertialForce.x,-7.4+inertialForce.y*.28,inertialForce.z);
    connections.forEach(connection=>{
      try{
        simulateHangingConnection(connection,delta,gravity,relationshipViewer.elapsed);
      }catch(error){
        console.warn('Skipped one invalid hanging relationship',connection?.relationship,error);
      }
    });

    let nodeCounter=0;
    nodeMap.forEach(node=>{
      simulateRelationshipNodeDrop(node,delta,relationshipViewer.elapsed,inertialForce);
      const targetTiltX=Math.max(-.18,Math.min(.18,inertialForce.z*.012));
      const targetTiltZ=Math.max(-.2,Math.min(.2,-inertialForce.x*.014));
      node.rotation.x+=(targetTiltX-node.rotation.x)*.08;
      node.rotation.z+=(targetTiltZ-node.rotation.z)*.08;
      node.userData.mesh.rotation.y=Math.sin(relationshipViewer.elapsed*.55+node.userData.swayPhase)*.06;
      node.userData.label.position.y=-.03+Math.sin(relationshipViewer.elapsed*.4+nodeCounter)*.025;
      nodeCounter+=1;
    });

    if(relationshipViewer.focusTarget){
      controls.target.lerp(relationshipViewer.focusTarget,.075);
      if(controls.target.distanceTo(relationshipViewer.focusTarget)<.02)relationshipViewer.focusTarget=null;
    }
    controls.update();renderer.render(scene,camera);
    relationshipViewer.animationId=requestAnimationFrame(animate);
  };
  relationshipViewer.animationId=requestAnimationFrame(animate);
}

function resetRelationshipCamera(){
  if(!relationshipViewer)return;
  relationshipViewer.camera.position.set(.7,7.1,22.8);
  relationshipViewer.controls.target.set(.2,1.65,0);
  relationshipViewer.inertialForce?.set(0,0,0);
  relationshipViewer.controls.update();
}

function destroyRelationshipNetwork(){
  if(!relationshipViewer)return;
  cancelAnimationFrame(relationshipViewer.animationId);
  relationshipViewer.resizeObserver?.disconnect();
  relationshipViewer.controls?.dispose();
  const canvas=relationshipViewer.renderer?.domElement;
  const listeners=relationshipViewer.listeners||{};
  canvas?.removeEventListener('pointerdown',listeners.onPointerDown);
  canvas?.removeEventListener('pointerup',listeners.onPointerUp);
  canvas?.removeEventListener('pointercancel',listeners.onPointerUp);
  canvas?.removeEventListener('pointermove',listeners.onPointerMove);
  canvas?.removeEventListener('pointerleave',listeners.onPointerLeave);
  canvas?.removeEventListener('wheel',listeners.onWheel);
  canvas?.removeEventListener('click',listeners.onClick);
  canvas?.removeEventListener('dblclick',listeners.onDoubleClick);
  relationshipViewer.scene?.traverse(object=>{
    object.geometry?.dispose?.();
    const materials=Array.isArray(object.material)?object.material:[object.material];
    materials.filter(Boolean).forEach(material=>{material.map?.dispose?.();material.dispose?.();});
    object.userData?.labelTexture?.dispose?.();
  });
  relationshipViewer.renderer?.dispose();
  relationshipViewer.renderer?.domElement?.remove();
  relationshipViewer=null;
}

function addInferredSeriesRelationships(){
  const existing=new Set(relationships.map(relationship=>[relationship.source,relationship.target].sort().join('|')));
  const families=new Map();
  archiveItems.forEach(item=>{
    const family=relationshipFamily(item);
    if(!families.has(family))families.set(family,[]);
    families.get(family).push(item);
  });
  families.forEach((items,family)=>{
    items.forEach((source,sourceIndex)=>items.forEach((target,targetIndex)=>{
      if(targetIndex<=sourceIndex)return;
      const key=[source.id,target.id].sort().join('|');
      if(existing.has(key))return;
      const separation=targetIndex-sourceIndex;
      relationships.push({
        source:source.id,
        target:target.id,
        category:family==='Window Profile'?'Geometry':'Fabrication',
        description:`Inferred ${family.toLowerCase()} series relationship based on shared prototype type, material system, and fabrication workflow.`,
        strength:Math.max(.46,.66-separation*.055)
      });
      existing.add(key);
    }));
  });
}


/* ============================================================
   ATLAS — CONSTELLATION REPROJECTED AS A GEOGRAPHIC FIELD
   ============================================================ */
function initAtlasTransitionField(){
  const field=$('#atlasTransitionField');
  if(!field||field.childElementCount)return;
  for(let index=0;index<14;index+=1){
    const ring=document.createElement('i');
    ring.className=`atlas-transition-ring ${index%3===0?'longitude':'latitude'}`;
    ring.style.setProperty('--index',index);
    ring.style.setProperty('--start-rot',`${-18+index*4.8}deg`);
    ring.style.setProperty('--end-rot',`${index*12.4}deg`);
    ring.style.setProperty('--tilt',`${-76+index*11.5}deg`);
    ring.style.setProperty('--delay',`${index*18}ms`);
    field.appendChild(ring);
  }
}

function latLonToVector3(lat,lon,radius){
  // Standard globe orientation used by Mundo.obj:
  // north = +Y, Greenwich = +X, east longitudes rotate toward +Z.
  const latitude=THREE.MathUtils.degToRad(lat);
  const longitude=THREE.MathUtils.degToRad(lon);
  const cosLatitude=Math.cos(latitude);
  return new THREE.Vector3(
    radius*cosLatitude*Math.cos(longitude),
    radius*Math.sin(latitude),
    radius*cosLatitude*Math.sin(longitude)
  );
}

function makeAtlasLine(points,material,closed=false){
  const geometry=new THREE.BufferGeometry().setFromPoints(points);
  return closed?new THREE.LineLoop(geometry,material):new THREE.Line(geometry,material);
}

function makeAtlasLabel(item){
  const canvas=document.createElement('canvas');
  canvas.width=640;canvas.height=128;
  const context=canvas.getContext('2d');
  context.clearRect(0,0,canvas.width,canvas.height);
  context.font='600 28px Arial';
  context.fillStyle='rgba(238,240,237,.96)';
  context.fillText(item.atlas.city.toUpperCase(),18,39);
  context.font='19px "Courier New"';
  context.fillStyle='rgba(155,164,168,.92)';
  context.fillText(item.archiveCode,18,72);
  const texture=new THREE.CanvasTexture(canvas);
  texture.colorSpace=THREE.SRGBColorSpace;
  texture.minFilter=THREE.LinearFilter;
  const material=new THREE.SpriteMaterial({map:texture,transparent:true,depthWrite:false,opacity:.82});
  const sprite=new THREE.Sprite(material);
  sprite.scale.set(2.7,.54,1);
  sprite.userData.labelTexture=texture;
  return sprite;
}

function atlasContinentOutlines(){
  return [
    [[72,-168],[65,-150],[58,-138],[53,-128],[48,-124],[42,-124],[35,-119],[28,-114],[23,-105],[19,-97],[25,-82],[31,-81],[39,-74],[46,-67],[54,-60],[61,-65],[68,-81],[72,-102],[70,-125],[72,-145],[72,-168]],
    [[13,-81],[7,-78],[-3,-80],[-13,-76],[-23,-70],[-35,-72],[-51,-68],[-55,-61],[-44,-52],[-28,-48],[-12,-45],[1,-50],[9,-61],[13,-81]],
    [[36,-10],[43,-9],[50,-5],[56,2],[61,10],[67,20],[70,32],[64,41],[56,41],[50,29],[44,28],[39,18],[36,9],[36,-10]],
    [[37,-17],[31,-10],[24,-16],[15,-17],[5,-10],[-5,-6],[-16,12],[-25,20],[-35,18],[-35,31],[-26,39],[-10,50],[5,48],[18,40],[30,32],[36,15],[37,-17]],
    [[72,33],[66,55],[58,73],[52,92],[44,112],[35,122],[24,121],[18,109],[8,104],[1,114],[-7,119],[-11,105],[-3,91],[8,78],[20,66],[30,52],[40,42],[52,36],[63,31],[72,33]],
    [[-11,112],[-18,115],[-25,113],[-35,116],[-43,132],[-39,147],[-27,153],[-17,146],[-12,132],[-11,112]],
    [[82,-73],[78,-50],[70,-31],[61,-43],[61,-58],[70,-70],[82,-73]]
  ];
}


function collectAtlasGeoJsonRings(input,rings=[]){
  if(!input)return rings;
  if(input.type==='FeatureCollection'){
    input.features?.forEach(feature=>collectAtlasGeoJsonRings(feature,rings));
    return rings;
  }
  if(input.type==='Feature')return collectAtlasGeoJsonRings(input.geometry,rings);
  if(input.type==='GeometryCollection'){
    input.geometries?.forEach(geometry=>collectAtlasGeoJsonRings(geometry,rings));
    return rings;
  }
  if(input.type==='Polygon'){
    input.coordinates?.forEach(ring=>rings.push(ring));
    return rings;
  }
  if(input.type==='MultiPolygon'){
    input.coordinates?.forEach(polygon=>polygon.forEach(ring=>rings.push(ring)));
  }
  return rings;
}

function splitAtlasRingAtDateline(ring){
  const chunks=[];
  let current=[];
  let previousLon=null;
  ring.forEach(coordinate=>{
    const lon=Number(coordinate?.[0]);
    const lat=Number(coordinate?.[1]);
    if(!Number.isFinite(lon)||!Number.isFinite(lat))return;
    if(previousLon!==null&&Math.abs(lon-previousLon)>180){
      if(current.length>1)chunks.push(current);
      current=[];
    }
    current.push([lon,lat]);
    previousLon=lon;
  });
  if(current.length>1)chunks.push(current);
  return chunks;
}

async function addAccurateAtlasCoastlines(globeGroup,radius,material){
  try{
    const response=await fetch('./assets/data/world-land-ne110.geojson',{cache:'force-cache'});
    if(!response.ok)throw new Error(`Coastline request failed (${response.status})`);
    const geojson=await response.json();
    const rings=collectAtlasGeoJsonRings(geojson);
    if(!rings.length)throw new Error('Coastline dataset contained no polygon rings');

    const coastlineGroup=new THREE.Group();
    coastlineGroup.name='natural-earth-coastlines';
    rings.forEach(ring=>{
      splitAtlasRingAtDateline(ring).forEach(chunk=>{
        if(chunk.length<2)return;
        const points=chunk.map(([lon,lat])=>latLonToVector3(lat,lon,radius+.026));
        coastlineGroup.add(makeAtlasLine(points,material,false));
      });
    });
    globeGroup.add(coastlineGroup);
    return true;
  }catch(error){
    console.warn('[HKS] Accurate Atlas coastlines could not load; using abstract fallback.',error);
    return false;
  }
}

function createAtlasPointMaterial({size=1.15,opacity=.7}={}){
  return new THREE.ShaderMaterial({
    transparent:true,
    depthWrite:false,
    depthTest:true,
    blending:THREE.NormalBlending,
    uniforms:{
      uTime:{value:0},
      uSize:{value:size},
      uOpacity:{value:opacity}
    },
    vertexShader:`
      attribute float aPhase;
      uniform float uTime;
      uniform float uSize;
      varying float vPulse;
      void main(){
        vec4 mvPosition=modelViewMatrix*vec4(position,1.0);
        float pulse=.94+.06*sin(uTime*.52+aPhase);
        vPulse=pulse;
        gl_PointSize=max(.55,uSize*pulse*(7.0/max(1.0,-mvPosition.z)));
        gl_Position=projectionMatrix*mvPosition;
      }
    `,
    fragmentShader:`
      uniform float uOpacity;
      varying float vPulse;
      void main(){
        vec2 centered=gl_PointCoord-vec2(.5);
        float distanceFromCenter=length(centered);
        if(distanceFromCenter>.5)discard;
        float alpha=1.0-smoothstep(.20,.5,distanceFromCenter);
        gl_FragColor=vec4(vec3(1.0),uOpacity*alpha*(.94+.06*vPulse));
      }
    `
  });
}

function seededAtlasRandom(seed=1){
  let value=seed>>>0;
  return ()=>{
    value=(value*1664525+1013904223)>>>0;
    return value/4294967296;
  };
}

function buildAtlasPointGeometry(sourceGeometry,extraSamples=0,seed=1,includeVertices=false){
  const geometry=sourceGeometry?.clone?.();
  const position=geometry?.getAttribute?.('position');
  if(!position)return new THREE.BufferGeometry();
  const index=geometry.index;
  const random=seededAtlasRandom(seed);
  const pointValues=[];
  const phases=[];

  // Sampling the original mesh vertices exposed triangulation seams and
  // sculpting creases. Uniform triangle samples create a clean point field.
  if(includeVertices){
    for(let i=0;i<position.count;i+=1){
      pointValues.push(position.getX(i),position.getY(i),position.getZ(i));
      phases.push(random()*Math.PI*2);
    }
  }

  const triangleCount=index?Math.floor(index.count/3):Math.floor(position.count/3);
  const a=new THREE.Vector3(),b=new THREE.Vector3(),c=new THREE.Vector3(),sample=new THREE.Vector3();
  for(let sampleIndex=0;sampleIndex<extraSamples&&triangleCount>0;sampleIndex+=1){
    const triangle=Math.floor(random()*triangleCount);
    const ia=index?index.getX(triangle*3):triangle*3;
    const ib=index?index.getX(triangle*3+1):triangle*3+1;
    const ic=index?index.getX(triangle*3+2):triangle*3+2;
    a.fromBufferAttribute(position,ia);
    b.fromBufferAttribute(position,ib);
    c.fromBufferAttribute(position,ic);
    let r1=Math.sqrt(random());
    let r2=random();
    sample.copy(a).multiplyScalar(1-r1)
      .addScaledVector(b,r1*(1-r2))
      .addScaledVector(c,r1*r2);
    pointValues.push(sample.x,sample.y,sample.z);
    phases.push(random()*Math.PI*2);
  }

  const pointGeometry=new THREE.BufferGeometry();
  pointGeometry.setAttribute('position',new THREE.Float32BufferAttribute(pointValues,3));
  pointGeometry.setAttribute('aPhase',new THREE.Float32BufferAttribute(phases,1));
  geometry.dispose?.();
  return pointGeometry;
}

async function loadAtlasEarthModel(globeGroup,radius){
  try{
    const loader=new OBJLoader();
    const earth=await new Promise((resolve,reject)=>loader.load(
      './assets/models/earth/Mundo.obj',
      resolve,
      undefined,
      reject
    ));

    const earthMeshes=[];
    const pointMaterials=[];
    const surfaceMaterials=[];
    earth.traverse(object=>{
      if(!object.isMesh)return;
      earthMeshes.push(object);
      const sourceMaterialNames=(Array.isArray(object.material)?object.material:[object.material])
        .map(material=>material?.name||'')
        .join(' ');
      const objectName=`${object.name||''} ${object.parent?.name||''} ${sourceMaterialNames}`.toLowerCase();
      const isOcean=objectName.includes('mundo_azul')||objectName.includes('azul')||objectName.includes('blinn3sg');
      object.geometry?.computeVertexNormals?.();

      // Keep a very light, smooth surface under the point cloud. No wireframe,
      // no triangle edges, and no flat shading—only a soft continuous shell.
      const surfaceMaterial=new THREE.MeshStandardMaterial({
        color:isOcean?0x76807f:0xdce1df,
        roughness:1,
        metalness:0,
        transparent:true,
        opacity:isOcean?.052:.075,
        depthWrite:false,
        side:THREE.DoubleSide,
        flatShading:false
      });
      surfaceMaterials.push(surfaceMaterial);
      object.material=surfaceMaterial;
      object.renderOrder=0;
      object.castShadow=false;
      object.receiveShadow=false;

      // Continents carry most of the cloud density. Oceans remain sparse so
      // coastlines and land masses stay legible rather than turning into noise.
      const pointGeometry=buildAtlasPointGeometry(
        object.geometry,
        isOcean?14000:118000,
        isOcean?104729:83717,
        false
      );
      const pointMaterial=createAtlasPointMaterial({
        size:isOcean?.56:1.08,
        opacity:isOcean?.13:.76
      });
      pointMaterials.push(pointMaterial);
      const cloud=new THREE.Points(pointGeometry,pointMaterial);
      cloud.name=isOcean?'atlas-ocean-point-cloud':'atlas-land-point-cloud';
      cloud.renderOrder=isOcean?2:3;
      object.add(cloud);
    });

    const bounds=new THREE.Box3().setFromObject(earth);
    const center=bounds.getCenter(new THREE.Vector3());
    const sphere=bounds.getBoundingSphere(new THREE.Sphere());
    if(!Number.isFinite(sphere.radius)||sphere.radius<=0)throw new Error('Earth OBJ has invalid bounds');
    earth.position.copy(center).multiplyScalar(-1);

    const earthRoot=new THREE.Group();
    earthRoot.name='mundo-refined-point-cloud-earth-root';
    earthRoot.scale.setScalar(radius/sphere.radius);
    earthRoot.add(earth);

    // A nearly invisible inner core improves depth without becoming a black orb.
    const depthCore=new THREE.Mesh(
      new THREE.SphereGeometry(radius*.978,64,42),
      new THREE.MeshBasicMaterial({
        color:0x0b1110,
        transparent:true,
        opacity:.018,
        depthWrite:false,
        side:THREE.FrontSide
      })
    );
    depthCore.name='atlas-point-cloud-depth-core';
    globeGroup.add(depthCore);
    globeGroup.add(earthRoot);
    globeGroup.updateMatrixWorld(true);
    return {root:earthRoot,surfaceMeshes:earthMeshes,pointMaterials,surfaceMaterials,depthCore};
  }catch(error){
    console.error('[HKS] Mundo.obj could not load; using point-cloud sphere fallback.',error);
    const fallbackGeometry=new THREE.SphereGeometry(radius,128,80);
    const fallbackMaterial=createAtlasPointMaterial({size:.72,opacity:.52});
    const fallback=new THREE.Points(fallbackGeometry,fallbackMaterial);
    fallback.name='atlas-point-cloud-earth-fallback';
    globeGroup.add(fallback);
    return {root:fallback,surfaceMeshes:[],pointMaterials:[fallbackMaterial],surfaceMaterials:[],depthCore:null};
  }
}

function projectAtlasPointToEarthSurface(direction,globeGroup,earthData,fallbackRadius,offset=.018){
  const unit=direction.clone().normalize();
  const meshes=earthData?.surfaceMeshes||[];
  if(meshes.length){
    globeGroup.updateMatrixWorld(true);
    const originLocal=unit.clone().multiplyScalar(fallbackRadius*3.2);
    const centerLocal=new THREE.Vector3();
    const originWorld=globeGroup.localToWorld(originLocal.clone());
    const centerWorld=globeGroup.localToWorld(centerLocal.clone());
    const raycaster=new THREE.Raycaster(originWorld,centerWorld.sub(originWorld).normalize(),0,fallbackRadius*8);
    const hit=raycaster.intersectObjects(meshes,true)[0];
    if(hit?.point){
      const localPoint=globeGroup.worldToLocal(hit.point.clone());
      return localPoint.add(localPoint.clone().normalize().multiplyScalar(offset));
    }
  }
  return unit.multiplyScalar(fallbackRadius+offset);
}

function makeAtlasArc(sourceItem,targetItem,relationship,radius,index,surfacePoints){
  const source=(surfacePoints?.get(sourceItem.id)||latLonToVector3(sourceItem.atlas.lat,sourceItem.atlas.lon,radius)).clone();
  const target=(surfacePoints?.get(targetItem.id)||latLonToVector3(targetItem.atlas.lat,targetItem.atlas.lon,radius)).clone();
  const sourceDirection=source.clone().normalize();
  const targetDirection=target.clone().normalize();
  const angle=sourceDirection.angleTo(targetDirection);
  const axis=new THREE.Vector3().crossVectors(sourceDirection,targetDirection);
  const canRotate=axis.lengthSq()>1e-8;
  if(canRotate)axis.normalize();
  const height=.055+angle*.13+relationship.strength*.045;
  const points=[];
  for(let step=0;step<=72;step+=1){
    const t=step/72;
    const direction=canRotate
      ? sourceDirection.clone().applyAxisAngle(axis,angle*t)
      : sourceDirection.clone().lerp(targetDirection,t).normalize();
    const surfaceRadius=THREE.MathUtils.lerp(source.length(),target.length(),t);
    direction.multiplyScalar(surfaceRadius+.014+Math.sin(Math.PI*t)*height);
    points.push(direction);
  }
  const color=relationshipCategoryColor(relationship.category);
  const material=new THREE.LineBasicMaterial({color,transparent:true,opacity:.2,depthWrite:false});
  const line=makeAtlasLine(points,material,false);
  line.userData={relationship,source:relationship.source,target:relationship.target,baseOpacity:.2};
  const signal=new THREE.Mesh(
    new THREE.SphereGeometry(.018,10,8),
    new THREE.MeshBasicMaterial({color,transparent:true,opacity:.78,depthWrite:false})
  );
  signal.userData.offset=(index*.173)%1;
  signal.position.copy(points[0]);
  return {line,signal,points,relationship};
}

function updateAtlasSelection(id){
  if(!atlasViewer)return;
  atlasViewer.selectedId=id;
  const item=archiveItems.find(entry=>entry.id===id);
  atlasViewer.markers.forEach((marker,markerId)=>{
    const selected=markerId===id;
    marker.userData.core.material.opacity=selected?1:.82;
    marker.userData.halo.material.opacity=selected?.88:.24;
    marker.userData.label.material.opacity=selected?1:.72;
    marker.userData.targetScale=selected?1.32:1;
  });
  atlasViewer.arcs.forEach(arc=>{
    const connected=arc.relationship.source===id||arc.relationship.target===id;
    arc.line.material.opacity=connected?.72:.075;
    arc.line.material.color.setHex(connected?0xdde8f2:relationshipCategoryColor(arc.relationship.category));
    arc.signal.material.opacity=connected?.95:.18;
  });
  if(item){
    $('#atlasSelectedCity').textContent=`${item.atlas.city}, ${item.atlas.country}`;
    $('#atlasSelectedProject').textContent=`${item.archiveCode} · ${item.title}`;
    $('#atlasSelectedCoordinates').textContent=`${Math.abs(item.atlas.lat).toFixed(2)}° ${item.atlas.lat>=0?'N':'S'} · ${Math.abs(item.atlas.lon).toFixed(2)}° ${item.atlas.lon>=0?'E':'W'}`;
  }
}

function focusAtlasLocation(id,openPanel=false){
  if(!atlasViewer){atlasPendingFocus={id,openPanel};return;}
  const item=archiveItems.find(entry=>entry.id===id);
  const marker=atlasViewer.markers.get(id);
  if(!item||!marker)return;
  updateAtlasSelection(id);
  atlasViewer.globeGroup.updateMatrixWorld(true);
  const worldPoint=marker.position.clone().applyMatrix4(atlasViewer.globeGroup.matrixWorld).normalize();
  atlasViewer.cameraGoal=worldPoint.multiplyScalar(7.05);
  atlasViewer.targetGoal=new THREE.Vector3(0,0,0);
  atlasViewer.pauseUntil=performance.now()+4200;
  if(openPanel)window.setTimeout(()=>openDetail(id),360);
}

function showItemOnAtlas(id){
  atlasPendingFocus={id,openPanel:false};
  closeDetail(false);
  if(state.view==='atlas')focusAtlasLocation(id,false);
  else setView('atlas');
}

async function initAtlasView(){
  const host=$('#atlasStage');
  const status=$('#atlasStatus');
  if(!host||state.view!=='atlas')return;
  if(atlasViewer?.host===host){
    atlasViewer.resize();
    if(atlasPendingFocus){const pending=atlasPendingFocus;atlasPendingFocus=null;focusAtlasLocation(pending.id,pending.openPanel);}
    return;
  }
  if(status){status.textContent='Assembling geographic field…';status.classList.remove('loaded','error');}
  destroyAtlasView();
  try{await ensureThree();}
  catch(error){
    console.error('Atlas failed to load',error);
    if(status){status.textContent='The geographic field could not load. Check the connection and refresh.';status.classList.add('error');}
    return;
  }
  if(state.view!=='atlas'||!document.body.contains(host))return;

  const scene=new THREE.Scene();
  scene.background=new THREE.Color(0x080a0a);
  scene.fog=new THREE.FogExp2(0x080a0a,.035);
  const camera=new THREE.PerspectiveCamera(40,1,.1,100);
  camera.position.set(0,.35,8.5);
  const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});
  renderer.setPixelRatio(Math.min(window.devicePixelRatio,1.8));
  renderer.outputColorSpace=THREE.SRGBColorSpace;
  host.innerHTML='';host.appendChild(renderer.domElement);

  const controls=new OrbitControls(camera,renderer.domElement);
  controls.enableDamping=true;controls.dampingFactor=.055;
  controls.enablePan=true;controls.screenSpacePanning=true;
  controls.minDistance=4.3;controls.maxDistance=15;
  controls.target.set(0,0,0);controls.update();

  scene.add(new THREE.HemisphereLight(0xd9e1e1,0x070909,1.35));
  const key=new THREE.DirectionalLight(0xe8ece8,1.45);key.position.set(5,5,7);scene.add(key);
  const rim=new THREE.DirectionalLight(0x71899d,1.05);rim.position.set(-7,1,-5);scene.add(rim);

  const globeGroup=new THREE.Group();
  globeGroup.rotation.set(.08,-.52,0);
  scene.add(globeGroup);
  const radius=2.66;
  const earthData=await loadAtlasEarthModel(globeGroup,radius);
  if(state.view!=='atlas'||!document.body.contains(host))return;

  // Clean geographic outlines—coastlines only, never mesh triangulation.
  const coastlineMaterial=new THREE.LineBasicMaterial({
    color:0xf1f3f0,
    transparent:true,
    opacity:.24,
    depthWrite:false
  });
  await addAccurateAtlasCoastlines(globeGroup,radius,coastlineMaterial);

  // A restrained graticule remains above the supplied model so project
  // coordinates and relationship arcs still read like an archive instrument.
  const atmosphere=new THREE.Mesh(
    new THREE.SphereGeometry(radius*1.04,64,40),
    new THREE.MeshBasicMaterial({color:0xbac6ca,transparent:true,opacity:.026,side:THREE.BackSide,depthWrite:false})
  );
  globeGroup.add(atmosphere);
  const gridMaterial=new THREE.LineBasicMaterial({color:0xcbd2cf,transparent:true,opacity:.022,depthWrite:false});
  for(let lat=-75;lat<=75;lat+=15){
    const points=[];
    for(let lon=-180;lon<180;lon+=4)points.push(latLonToVector3(lat,lon,radius+.028));
    globeGroup.add(makeAtlasLine(points,gridMaterial,true));
  }
  for(let lon=-165;lon<180;lon+=15){
    const points=[];
    for(let lat=-88;lat<=88;lat+=3)points.push(latLonToVector3(lat,lon,radius+.03));
    globeGroup.add(makeAtlasLine(points,gridMaterial,false));
  }

  const markers=new Map();
  const pickables=[];
  const surfacePoints=new Map();
  archiveItems.forEach((item,index)=>{
    const group=new THREE.Group();
    const direction=latLonToVector3(item.atlas.lat,item.atlas.lon,1).normalize();
    const point=projectAtlasPointToEarthSurface(direction,globeGroup,earthData,radius,.014);
    surfacePoints.set(item.id,point.clone());
    group.position.copy(point);
    group.quaternion.setFromUnitVectors(new THREE.Vector3(0,1,0),point.clone().normalize());
    const stem=new THREE.Line(
      new THREE.BufferGeometry().setFromPoints([new THREE.Vector3(0,0,0),new THREE.Vector3(0,.085,0)]),
      new THREE.LineBasicMaterial({color:0xe2e6e3,transparent:true,opacity:.56})
    );
    const core=new THREE.Mesh(
      new THREE.SphereGeometry(.052,18,14),
      new THREE.MeshStandardMaterial({color:0xf2f3ef,emissive:0x879aa2,emissiveIntensity:.48,roughness:.42,transparent:true,opacity:.9})
    );
    core.position.y=.098;core.userData.itemId=item.id;
    const halo=new THREE.Mesh(
      new THREE.TorusGeometry(.091,.006,8,34),
      new THREE.MeshBasicMaterial({color:0xe3e9e9,transparent:true,opacity:.28,depthWrite:false})
    );
    halo.rotation.x=Math.PI/2;halo.position.y=.1;
    const label=makeAtlasLabel(item);label.position.set(.105,.225,0);label.scale.multiplyScalar(.78);
    group.add(stem,halo,core,label);
    group.scale.setScalar(.001);
    group.userData={itemId:item.id,core,halo,label,introDelay:index*.055,targetScale:1};
    globeGroup.add(group);markers.set(item.id,group);pickables.push(core);
  });

  const arcs=[];
  relationships.forEach((relationship,index)=>{
    const source=archiveItems.find(item=>item.id===relationship.source);
    const target=archiveItems.find(item=>item.id===relationship.target);
    if(!source||!target)return;
    const arc=makeAtlasArc(source,target,relationship,radius,index,surfacePoints);
    globeGroup.add(arc.line,arc.signal);arcs.push(arc);
  });

  const starsGeometry=new THREE.BufferGeometry();
  const stars=[];
  for(let i=0;i<650;i+=1){
    const vector=new THREE.Vector3().randomDirection().multiplyScalar(10+Math.random()*25);
    stars.push(vector.x,vector.y,vector.z);
  }
  starsGeometry.setAttribute('position',new THREE.Float32BufferAttribute(stars,3));
  scene.add(new THREE.Points(starsGeometry,new THREE.PointsMaterial({color:0xb8c0bd,size:.018,transparent:true,opacity:.28,depthWrite:false})));

  const raycaster=new THREE.Raycaster();
  const pointer=new THREE.Vector2();
  const tooltip=$('#atlasTooltip');
  const setPointer=event=>{
    const rect=renderer.domElement.getBoundingClientRect();
    pointer.x=((event.clientX-rect.left)/rect.width)*2-1;
    pointer.y=-((event.clientY-rect.top)/rect.height)*2+1;
  };
  const onPointerMove=event=>{
    setPointer(event);raycaster.setFromCamera(pointer,camera);
    const hit=raycaster.intersectObjects(pickables,false)[0];
    const id=hit?.object?.userData?.itemId||null;
    renderer.domElement.style.cursor=id?'pointer':'grab';
    if(tooltip){
      if(id){
        const item=archiveItems.find(entry=>entry.id===id);
        const rect=renderer.domElement.getBoundingClientRect();
        tooltip.innerHTML=`<strong>${item.atlas.city}</strong><span>${item.archiveCode}<br>${item.title}</span>`;
        tooltip.style.left=`${event.clientX-rect.left}px`;tooltip.style.top=`${event.clientY-rect.top}px`;
        tooltip.classList.add('visible');tooltip.setAttribute('aria-hidden','false');
      }else{tooltip.classList.remove('visible');tooltip.setAttribute('aria-hidden','true');}
    }
  };
  const onClick=event=>{
    setPointer(event);raycaster.setFromCamera(pointer,camera);
    const hit=raycaster.intersectObjects(pickables,false)[0];
    const id=hit?.object?.userData?.itemId||null;
    if(id){focusAtlasLocation(id,false);window.setTimeout(()=>openDetail(id),280);}
  };
  const onPointerLeave=()=>tooltip?.classList.remove('visible');
  const pauseAuto=()=>{if(atlasViewer)atlasViewer.pauseUntil=performance.now()+3600;};
  renderer.domElement.addEventListener('pointermove',onPointerMove);
  renderer.domElement.addEventListener('pointerleave',onPointerLeave);
  renderer.domElement.addEventListener('click',onClick);
  renderer.domElement.addEventListener('pointerdown',pauseAuto);
  renderer.domElement.addEventListener('wheel',pauseAuto,{passive:true});

  const resize=()=>{
    const width=Math.max(1,host.clientWidth),height=Math.max(1,host.clientHeight);
    renderer.setSize(width,height,false);camera.aspect=width/height;camera.updateProjectionMatrix();
  };
  const resizeObserver=new ResizeObserver(resize);resizeObserver.observe(host);resize();
  atlasViewer={host,scene,camera,renderer,controls,globeGroup,earthModel:earthData.root,earthData,surfacePoints,markers,arcs,pickables,resizeObserver,resize,animationId:null,elapsed:0,selectedId:null,cameraGoal:null,targetGoal:null,pauseUntil:0,listeners:{onPointerMove,onPointerLeave,onClick,pauseAuto}};
  $('#atlasResetView')?.addEventListener('click',resetAtlasCamera);
  if(status){status.classList.add('loaded');status.textContent='';}

  let last=performance.now();
  const animate=now=>{
    if(!atlasViewer||atlasViewer.renderer!==renderer)return;
    const delta=Math.min((now-last)/1000,.05);last=now;atlasViewer.elapsed+=delta;
    if(now>atlasViewer.pauseUntil&&!atlasViewer.cameraGoal)globeGroup.rotation.y+=delta*.055;
    earthData.pointMaterials?.forEach(material=>{
      if(material?.uniforms?.uTime)material.uniforms.uTime.value=atlasViewer.elapsed;
    });
    markers.forEach(marker=>{
      const intro=Math.max(0,Math.min(1,(atlasViewer.elapsed-marker.userData.introDelay)/.72));
      const overshoot=intro<1?1+Math.sin(intro*Math.PI*2.3)*(1-intro)*.19:1;
      const desired=(marker.userData.targetScale||1)*intro*overshoot;
      const current=marker.scale.x;
      marker.scale.setScalar(current+(desired-current)*.14);
      marker.userData.halo.rotation.z+=delta*.62;
    });
    arcs.forEach((arc,index)=>{
      // Keep one malformed or not-yet-ready arc from crashing the entire Atlas.
      const points=Array.isArray(arc?.points)?arc.points:[];
      const signal=arc?.signal;
      if(!signal||points.length===0)return;

      const rawOffset=Number(signal.userData?.offset);
      const offset=Number.isFinite(rawOffset)?rawOffset:0;
      const speed=.055+(index%4)*.008;
      const rawT=atlasViewer.elapsed*speed+offset;
      const t=Number.isFinite(rawT)?((rawT%1)+1)%1:0;
      const scaled=t*Math.max(0,points.length-1);
      const low=Math.max(0,Math.min(points.length-1,Math.floor(scaled)));
      const high=Math.max(0,Math.min(points.length-1,low+1));
      const start=points[low];
      const end=points[high]||start;
      if(!start?.isVector3)return;

      signal.position.copy(start);
      if(end?.isVector3&&high!==low){
        signal.position.lerp(end,Math.max(0,Math.min(1,scaled-low)));
      }
    });
    if(atlasViewer.cameraGoal){
      camera.position.lerp(atlasViewer.cameraGoal,.075);
      controls.target.lerp(atlasViewer.targetGoal||new THREE.Vector3(),.075);
      if(camera.position.distanceTo(atlasViewer.cameraGoal)<.035)atlasViewer.cameraGoal=null;
    }
    controls.update();renderer.render(scene,camera);
    atlasViewer.animationId=requestAnimationFrame(animate);
  };
  // Draw the globe once before animated arc signals begin updating.
  controls.update();
  renderer.render(scene,camera);
  atlasViewer.animationId=requestAnimationFrame(animate);
  if(atlasPendingFocus){const pending=atlasPendingFocus;atlasPendingFocus=null;window.setTimeout(()=>focusAtlasLocation(pending.id,pending.openPanel),120);}
}

function resetAtlasCamera(){
  if(!atlasViewer)return;
  atlasViewer.cameraGoal=null;atlasViewer.targetGoal=null;atlasViewer.selectedId=null;
  atlasViewer.camera.position.set(0,.35,8.5);atlasViewer.controls.target.set(0,0,0);
  atlasViewer.globeGroup.rotation.set(.08,-.52,0);atlasViewer.pauseUntil=performance.now()+1200;
  atlasViewer.controls.update();
  $('#atlasSelectedCity').textContent='Global archive';
  $('#atlasSelectedProject').textContent='Select a project marker';
  $('#atlasSelectedCoordinates').textContent='Drag to orbit · Scroll to zoom';
  atlasViewer.markers.forEach(marker=>{marker.userData.targetScale=1;marker.userData.core.material.opacity=.82;marker.userData.halo.material.opacity=.24;marker.userData.label.material.opacity=.72;});
  atlasViewer.arcs.forEach(arc=>{arc.line.material.opacity=.16;arc.line.material.color.setHex(relationshipCategoryColor(arc.relationship.category));arc.signal.material.opacity=.7;});
}

function destroyAtlasView(){
  if(!atlasViewer)return;
  cancelAnimationFrame(atlasViewer.animationId);
  atlasViewer.resizeObserver?.disconnect();atlasViewer.controls?.dispose();
  const canvas=atlasViewer.renderer?.domElement;const listeners=atlasViewer.listeners||{};
  canvas?.removeEventListener('pointermove',listeners.onPointerMove);
  canvas?.removeEventListener('pointerleave',listeners.onPointerLeave);
  canvas?.removeEventListener('click',listeners.onClick);
  canvas?.removeEventListener('pointerdown',listeners.pauseAuto);
  canvas?.removeEventListener('wheel',listeners.pauseAuto);
  atlasViewer.scene?.traverse(object=>{
    object.geometry?.dispose?.();
    const materials=Array.isArray(object.material)?object.material:[object.material];
    materials.filter(Boolean).forEach(material=>{material.map?.dispose?.();material.dispose?.();});
  });
  atlasViewer.renderer?.dispose();atlasViewer.renderer?.domElement?.remove();atlasViewer=null;
}

function renderTimeline(){
  const years=[...new Set(archiveItems.map(item=>item.year))].sort((a,b)=>b-a);
  $('#timelineList').innerHTML=years.map(year=>`<section class="year-group"><div class="year-label">${year}</div><div class="year-items">${archiveItems.filter(item=>item.year===year).map(item=>`<article class="timeline-item" data-id="${item.id}"><small>${item.archiveCode}</small><h3>${item.title}</h3><p>${item.location} · ${item.material}</p></article>`).join('')}</div></section>`).join('');
  $$('.timeline-item').forEach(card=>card.onclick=()=>focusItem(card.dataset.id));
}

function openSearch(){
  const overlay=$('#searchOverlay');
  overlay.classList.add('open');
  overlay.setAttribute('aria-hidden','false');
  $('#searchInput').value='';
  renderSearch('');
  setTimeout(()=>$('#searchInput').focus(),30);
}
function closeSearch(){
  const overlay=$('#searchOverlay');
  overlay.classList.remove('open');
  overlay.setAttribute('aria-hidden','true');
}
function renderSearch(query){
  query=query.toLowerCase().trim();
  const results=archiveItems.filter(item=>!query||[item.title,item.material,item.location,item.typology,item.archiveCode,item.atlas?.city,item.atlas?.country].join(' ').toLowerCase().includes(query)).slice(0,20);
  $('#searchResults').innerHTML=results.map(item=>`<div class="search-result" data-id="${item.id}"><small>${item.archiveCode}</small><strong>${item.title}</strong><span>${item.year} · ${item.location}</span></div>`).join('');
  $$('.search-result').forEach(result=>result.onclick=()=>{
    closeSearch();
    focusItem(result.dataset.id);
  });
}

$('#searchButton').onclick=openSearch;
$('#closeSearch').onclick=closeSearch;
$('#searchInput').oninput=event=>renderSearch(event.target.value);
document.addEventListener('keydown',event=>{
  if(event.key==='Escape'){
    closeSearch();
    closeDetail(true);
  }
  if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==='k'){
    event.preventDefault();
    openSearch();
  }
});

addInferredSeriesRelationships();
initAtlasTransitionField();
renderObservatory();
renderFilters();
renderRelationships();
renderTimeline();
updateOrbitPositions();

console.info('[HKS] Atlas v4.7 loaded — refined point-cloud Earth + smooth surface build');
